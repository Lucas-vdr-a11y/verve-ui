# Verve UI — Components

> Auto-generated by `scripts/generate-manifest.mjs`. Do not edit by hand.

**660** components across **28** categories.

## Categories

- [blocks](#blocks) — 28
- [charts](#charts) — 21
- [chat](#chat) — 16
- [commerce](#commerce) — 18
- [cursor](#cursor) — 9
- [data-display](#data-display) — 44
- [devices](#devices) — 8
- [disclosure](#disclosure) — 9
- [dnd](#dnd) — 9
- [feedback](#feedback) — 26
- [flair](#flair) — 47
- [fx](#fx) — 32
- [gamification](#gamification) — 12
- [inputs](#inputs) — 57
- [layout](#layout) — 28
- [loaders](#loaders) — 24
- [media](#media) — 21
- [motion](#motion) — 9
- [navigation](#navigation) — 29
- [overlay](#overlay) — 16
- [patterns](#patterns) — 12
- [scheduling](#scheduling) — 9
- [showcase](#showcase) — 36
- [templates](#templates) — 8
- [texteffects](#texteffects) — 37
- [threed](#threed) — 9
- [typography](#typography) — 20
- [utility](#utility) — 66

## blocks

_28 components._

- **AnnouncementBar** — AnnouncementBar — a slim, centered, marketing-flavored strip for the top of a page. Supports a gradient/brand/neutral tone, an optional CTA, and an optional dismiss button. Hides itself once dismissed.
  - exports: `AnnouncementBarTone`, `AnnouncementBarProps`, `AnnouncementBar`
  - styles: yes
- **BlogPostCard** — BlogPostCard — cover image, category tag, title, excerpt and an author / date / read-time footer. Lifts on hover.
  - exports: `BlogPostCardProps`, `BlogPostCard`
  - styles: yes
- **ContactForm** — ContactForm — name, email, subject and message fields with required marks. Validation-ready (native required + aria-invalid hooks).
  - exports: `ContactSubmitValues`, `ContactFormProps`, `ContactForm`
  - styles: yes
- **CTABanner** — CTABanner — a call-to-action section with title, subtitle and action slots. Supports a brand gradient, subtle, or solid background and start/center align.
  - exports: `CTABannerVariant`, `CTABannerAlign`, `CTABannerProps`, `CTABanner`
  - styles: yes
- **EmptyCard** — EmptyCard — a dashed placeholder card for "add new" affordances. Renders as a button so it is keyboard-operable and clickable.
  - exports: `EmptyCardProps`, `EmptyCard`
  - styles: yes
- **EventCard** — EventCard — date badge, title, time + location with icons, an attendees slot and a CTA. Suited to event listings.
  - exports: `EventCardProps`, `EventCard`
  - styles: yes
- **FaqSection** — FaqSection — a heading/subtitle plus a self-contained accordion of Q&A. Single-open by default; set `allowMultiple` to keep several panels open.
  - exports: `FaqItem`, `FaqSectionProps`, `FaqSection`
  - styles: yes
- **FeatureCard** — FeatureCard — icon + title + description tile for feature grids. Lifts on hover; renders an optional link/CTA at the bottom.
  - exports: `FeatureCardProps`, `FeatureCard`
  - styles: yes
- **FeatureGrid** — FeatureGrid — a section with an optional eyebrow/title/subtitle header and a responsive grid of feature items. The feature-item look is composed inline.
  - exports: `FeatureGridItem`, `FeatureGridColumns`, `FeatureGridProps`, `FeatureGrid`
  - styles: yes
- **FileCard** — FileCard — file icon by type, filename, size, optional upload/download progress bar and download/remove actions.
  - exports: `FileKind`, `FileCardProps`, `FileCard`
  - styles: yes
- **HeroSection** — HeroSection — a complete marketing hero: eyebrow, big title, subtitle, primary and secondary CTA slots, an optional media/screenshot slot, and a trust-badges row. Supports a centered or split layout and subtle/gradient backgrounds.
  - exports: `HeroSectionLayout`, `HeroSectionBackground`, `HeroSectionProps`, `HeroSection`
  - styles: yes
- **KpiCard** — KpiCard — dashboard KPI tile with a label, big value, delta vs previous period, an inline SVG sparkline, an icon, and a period note.
  - exports: `KpiCardTrendDirection`, `KpiCardDelta`, `KpiCardProps`, `KpiCard`
  - styles: yes
- **LoginForm** — LoginForm — email + password sign-in block with remember-me, forgot-password link, optional social-login slot and an error area. Self-contained inputs.
  - exports: `LoginSubmitValues`, `LoginFormProps`, `LoginForm`
  - styles: yes
- **LogoCloud** — LogoCloud — a "trusted by" band: a title above a responsive, wrapping row of logo slots. Logos render grayscale-on-light and reveal color on hover.
  - exports: `LogoCloudProps`, `LogoCloud`
  - styles: yes
- **NewsletterCTA** — NewsletterCTA — a full-width newsletter call-to-action section with a title, subtitle, inline email + button, and a privacy note. Supports gradient, brand, or subtle backgrounds and idle/success/error states.
  - exports: `NewsletterCTAStatus`, `NewsletterCTAProps`, `NewsletterCTA`
  - styles: yes
- **NewsletterSignup** — NewsletterSignup — inline email + subscribe button with success/error states. Supports a compact inline layout and a richer card layout.
  - exports: `NewsletterStatus`, `NewsletterSignupProps`, `NewsletterSignup`
  - styles: yes
- **NotificationItem** — NotificationItem — a single notification row for lists and dropdowns: icon/avatar, title, description, timestamp, unread dot and actions.
  - exports: `NotificationItemProps`, `NotificationItem`
  - styles: yes
- **OnboardingChecklist** — OnboardingChecklist — a setup checklist card with an overall progress bar, "n of m complete" summary, optional dismiss, and collapsible completed steps.
  - exports: `OnboardingStep`, `OnboardingChecklistProps`, `OnboardingChecklist`
  - styles: no
- **PricingCard** — PricingCard — a single pricing plan tile with feature list and CTA. Use the `highlighted` variant to mark the recommended plan.
  - exports: `PricingFeature`, `PricingCardProps`, `PricingCard`
  - styles: yes
- **PricingTable** — PricingTable — a self-contained multi-plan pricing section. Renders a header, an optional billing-toggle slot, and a responsive row of stacked plan cards with a per-plan feature checklist and CTA. One plan may be highlighted.
  - exports: `PricingTablePlan`, `PricingTableProps`, `PricingTable`
  - styles: yes
- **ProductCard** — ProductCard — e-commerce tile: image, title, price, optional rating/badge, and an add-to-cart action. Lifts on hover.
  - exports: `ProductBadgeTone`, `ProductCardProps`, `ProductCard`
  - styles: yes
- **ProfileCard** — ProfileCard — avatar, identity, bio, optional stats row and action/social slots. Suited to team/member/profile grids.
  - exports: `ProfileStat`, `ProfileCardProps`, `ProfileCard`
  - styles: yes
- **SignupForm** — SignupForm — name, email, password (+ optional confirm), terms checkbox and an optional social-login slot. Self-contained inputs.
  - exports: `SignupSubmitValues`, `SignupFormProps`, `SignupForm`
  - styles: yes
- **StatCard** — StatCard — dashboard KPI tile: label, big value, optional delta/trend, icon and mini description.
  - exports: `StatCardTrendDirection`, `StatCardDelta`, `StatCardProps`, `StatCard`
  - styles: yes
- **StatsRow** — StatsRow — a band of key stats with optional dividers. Reads well placed directly under a hero. Renders as a list of figure/label pairs.
  - exports: `StatsRowItem`, `StatsRowProps`, `StatsRow`
  - styles: yes
- **TeamMemberCard** — TeamMemberCard — photo, name, role, bio and a row of social links. Suited to "meet the team" grids.
  - exports: `TeamMemberCardProps`, `TeamMemberCard`
  - styles: yes
- **TestimonialCard** — TestimonialCard — a quote with author identity and optional star rating.
  - exports: `TestimonialCardProps`, `TestimonialCard`
  - styles: yes
- **TimelineSection** — TimelineSection — a vertical or alternating timeline of milestones, ideal for an "our story" or roadmap section. Each milestone has a date, title, optional description, and an optional marker icon.
  - exports: `TimelineMilestone`, `TimelineSectionProps`, `TimelineSection`
  - styles: yes

## charts

_21 components._

- **ActivityRings** — ActivityRings (ProgressDonut) — concentric progress rings in the Apple Activity style: full circle, rounded caps, each value/max. Distinct from RadialBarChart by the rounded-cap rings drawn with stroke-dasharray. Values over 100% wrap visually as an overshoot ring.
  - exports: `ActivityRing`, `ActivityRingsProps`, `ActivityRings`
  - styles: yes
- **AreaChart** — AreaChart — filled-area variant of {@link LineChart}. Shipped as its own component so the area look (gradient fill under each series, smoothing on by default) is a first-class chart. It composes `LineChart` with `area` forced on. The gradient fill is defined per-series inside `LineChart` using token-driven `currentColor` stops.
  - exports: `AreaChartProps`, `AreaChart`
  - styles: yes
- **BarChart** — BarChart — vertical bars sized to the max value, responsive via `viewBox`. Tokens-only theming; hover highlight handled in CSS. Bars cycle their own tone or fall back to the chart `tone`.
  - exports: `BarDatum`, `BarChartProps`, `BarChart`
  - styles: yes
- **BoxPlot** — BoxPlot — one or more box-and-whisker plots computed from raw value series. Computes min/Q1/median/Q3/max (Tukey whiskers) plus 1.5·IQR outliers per series. Vertical or horizontal layout, tones, and a shared value axis.
  - exports: `BoxPlotSeries`, `BoxPlotProps`, `BoxPlot`
  - styles: yes
- **BulletChart** — BulletChart — compact KPI bullet graph: a measure bar over graded qualitative range bands with a target marker. Horizontal, pure SVG, responsive.
  - exports: `BulletBand`, `BulletChartProps`, `BulletChart`
  - styles: yes
- **CandlestickChart** — CandlestickChart — OHLC candles with high/low wicks and up/down coloring. Optional volume row beneath. Pure SVG, responsive via `viewBox`.
  - exports: `Candle`, `CandlestickChartProps`, `CandlestickChart`
  - styles: yes
- **ComboChart** — ComboChart — bars (primary axis) with a line overlay (secondary axis) on a shared category axis. Pure SVG, two independent y-scales, responsive via `viewBox`. Useful for e.g. revenue bars + margin% line.
  - exports: `ComboDatum`, `ComboChartProps`, `ComboChart`
  - styles: yes
- **DonutChart** — DonutChart — donut or pie chart computed from segment values. Pure SVG arc math; configurable `thickness` switches between donut and pie. Accessible: the SVG is `role="img"` with a summary label, and a visually-hidden data table mirrors the values for assistive tech.
  - exports: `DonutSegment`, `DonutChartProps`, `DonutChart`
  - styles: yes
- **Funnel** — Funnel — stacked trapezoid segments narrowing top-to-bottom, sized by value. Each stage's width is proportional to its value relative to the first stage; trapezoids connect consecutive widths. Percentage labels show share of the first stage or conversion from the previous stage.
  - exports: `FunnelStage`, `FunnelProps`, `Funnel`
  - styles: yes
- **GaugeChart** — GaugeChart — a 180° semicircular gauge with arc fill and optional needle. Pure SVG arc math, responsive via `viewBox`. Tone can switch on value via `thresholds`. The center shows the value (or custom content).
  - exports: `GaugeThreshold`, `GaugeChartProps`, `GaugeChart`
  - styles: yes
- **Heatmap** — Heatmap — grid of cells colored by intensity over a single tone token. Intensity is `(value - min) / (max - min)`, rendered via `color-mix` from the surface to the tone. Optional x/y axis labels, per-cell `<title>` tooltips, in-cell values, and a min→max legend scale.
  - exports: `HeatmapProps`, `Heatmap`
  - styles: yes
- **Histogram** — Histogram — distribution of raw `values` (auto-binned) or precomputed `bins`. Adjacent bar columns sized to the max count, an x-axis with edge ticks, and an optional smoothed density curve. Pure math, responsive via `viewBox`.
  - exports: `HistogramBin`, `HistogramProps`, `Histogram`
  - styles: yes
- **LineChart** — LineChart — single or multi-series line chart, pure SVG and responsive. Series cycle through the categorical token palette unless given an explicit `color`. Shared rendering also powers {@link AreaChart} via the `area` prop.
  - exports: `LinePoint`, `LineSeries`, `LineChartProps`, `LineChart`
  - styles: yes
- **PieChart** — PieChart — a full (no-hole) labelled pie computed from segment values. Pure SVG arc math. Slice labels render inside the slice or via leader lines outside the pie. Accessible: `role="img"` summary plus a visually-hidden data table.
  - exports: `PieSegment`, `PieChartProps`, `PieChart`
  - styles: yes
- **RadarChart** — RadarChart — spider/radar polygons over a set of shared axes. Each series becomes a filled polygon (with translucent fill + solid stroke); concentric grid rings and radial spokes provide scale. Pure trig, SSR-safe.
  - exports: `RadarSeries`, `RadarChartProps`, `RadarChart`
  - styles: yes
- **RadialBarChart** — RadialBarChart — concentric radial bars, one ring per item, each filled to `value / max` along the sweep. Pure SVG arc math; supports partial sweeps for a gauge-like layout and a center label.
  - exports: `RadialBarItem`, `RadialBarChartProps`, `RadialBarChart`
  - styles: yes
- **ScatterChart** — ScatterChart — points plotted on linear X/Y axes with ticks and optional bubble sizing via per-point `r`. Multiple series are colored from the palette.
  - exports: `ScatterPoint`, `ScatterSeries`, `ScatterChartProps`, `ScatterChart`
  - styles: yes
- **Sparkline** — Sparkline — a tiny, inline, axis-free line chart. Pure SVG, responsive via `viewBox` (scales to its container width while keeping `width`/`height` as the intrinsic aspect ratio).
  - exports: `SparklineProps`, `Sparkline`
  - styles: yes
- **StackedBarChart** — StackedBarChart — vertical bars built from multiple stacked series. Each row contributes one bar; series are stacked in array order using the categorical palette (or per-series tone/color). `stacked100` normalizes every bar to its own total for proportional comparison.
  - exports: `StackedBarSeries`, `StackedBarChartProps`, `StackedBarChart`
  - styles: yes
- **Treemap** — Treemap — nested rectangles sized by value via a squarified layout. Single level, pure SVG, tone/palette fills, labels and values inside cells.
  - exports: `TreemapDatum`, `TreemapProps`, `Treemap`
  - styles: yes
- **WaterfallChart** — WaterfallChart — running-total waterfall. Delta steps float between the previous and new running total (success for rises, danger for falls); total steps anchor from the baseline. Optional connectors link consecutive bars.
  - exports: `WaterfallStep`, `WaterfallChartProps`, `WaterfallChart`
  - styles: yes

## chat

_16 components._

- **ChatAttachment**
  - exports: `ChatAttachmentProps`, `ChatAttachment`
  - styles: yes
- **ChatBubble**
  - exports: `ChatRole`, `ChatBubbleTone`, `ChatStatus`, `ChatBubbleProps`, `ChatBubble`
  - styles: yes
- **ChatComposerToolbar**
  - exports: `ChatComposerToolbarProps`, `ChatComposerToolbar`
  - styles: yes
- **ChatHeader**
  - exports: `ChatHeaderStatus`, `ChatHeaderProps`, `ChatHeader`
  - styles: yes
- **ChatInput**
  - exports: `ChatInputProps`, `ChatInput`
  - styles: yes
- **ChatMessageList** — SSR-safe layout effect — falls back to a no-op effect on the server.
  - exports: `ChatMessageListProps`, `ChatMessageList`, `ChatDateSeparatorProps`, `ChatDateSeparator`
  - styles: yes
- **ChatScrollButton**
  - exports: `ChatScrollButtonProps`, `ChatScrollButton`
  - styles: yes
- **CodeMessage**
  - exports: `CodeMessageProps`, `CodeMessage`
  - styles: yes
- **ConversationList**
  - exports: `ConversationItem`, `ConversationListProps`, `ConversationList`
  - styles: yes
- **MessageActions**
  - exports: `MessageActionsProps`, `MessageActions`
  - styles: yes
- **MessageReactions**
  - exports: `Reaction`, `MessageReactionsProps`, `MessageReactions`
  - styles: yes
- **PromptBox**
  - exports: `PromptBoxProps`, `PromptBox`
  - styles: yes
- **StreamingText**
  - exports: `StreamingTextProps`, `StreamingText`
  - styles: yes
- **SuggestionChips**
  - exports: `SuggestionChip`, `SuggestionChipsProps`, `SuggestionChips`
  - styles: yes
- **TypingIndicator**
  - exports: `TypingIndicatorProps`, `TypingIndicator`
  - styles: yes
- **VoiceMessage**
  - exports: `VoiceMessageProps`, `VoiceMessage`
  - styles: yes

## commerce

_18 components._

- **AddToCartButton** — AddToCartButton — button with idle / loading / added states. The added state animates a check icon. State changes are announced via an aria-live region.
  - exports: `AddToCartState`, `AddToCartSize`, `AddToCartButtonProps`, `AddToCartButton`
  - styles: yes
- **CartItem** — CartItem — a cart line item: image, title, options, unit price, an inline quantity stepper, line total, and a remove button.
  - exports: `CartItemProps`, `CartItem`
  - styles: yes
- **CheckoutSummary** — CheckoutSummary — order summary panel: optional line items, subtotal, shipping, tax, discount, an emphasized total, plus optional promo + CTA slots.
  - exports: `CheckoutLine`, `CheckoutSummaryProps`, `CheckoutSummary`
  - styles: yes
- **ColorVariantSelector** — ColorVariantSelector — a radiogroup of color swatches. The selected swatch gets a ring; out-of-stock swatches show a diagonal strike. Swatch fill comes from arbitrary product data via inline style (not theming).
  - exports: `ColorVariantSelectorSize`, `ColorOption`, `ColorVariantSelectorProps`, `ColorVariantSelector`
  - styles: yes
- **CouponInput** — CouponInput — controlled promo-code input + apply button, with applied (success) and error states.
  - exports: `CouponInputProps`, `CouponInput`
  - styles: yes
- **DeliveryEstimate** — DeliveryEstimate — shows an estimated delivery date/window with a truck icon and an optional radiogroup of shipping options to pick from.
  - exports: `ShippingOption`, `DeliveryEstimateProps`, `DeliveryEstimate`
  - styles: yes
- **MiniCart** — MiniCart — compact cart panel content: line items (image, title, qty, price), subtotal and checkout / view-cart CTAs, with an empty state. This is the content block only — the overlay/dropdown shell is the caller's concern.
  - exports: `MiniCartLineItem`, `MiniCartProps`, `MiniCart`
  - styles: yes
- **PaymentMethods** — PaymentMethods — a radiogroup of selectable payment methods (card, PayPal, Apple Pay, Google Pay or custom) with icons. Keyboard operable.
  - exports: `PaymentMethodId`, `PaymentMethodOption`, `PaymentMethodsProps`, `PaymentMethods`
  - styles: yes
- **PriceTag** — PriceTag — a formatted price with optional strike-through original price and discount badge. On sale (original > current) it switches to sale styling.
  - exports: `PriceTagSize`, `PriceTagProps`, `PriceTag`
  - styles: yes
- **ProductGallery** — ProductGallery — a main image plus a thumbnail strip selector. Thumbnails are keyboard operable (arrow keys move, Enter/Space select). SSR-safe: zoom is a pure pointer-driven CSS transform with no window/document access at render.
  - exports: `ProductGalleryImage`, `ProductGalleryProps`, `ProductGallery`
  - styles: yes
- **QuantityStepper** — QuantityStepper — accessible − [n] + control for cart quantities. Works controlled (`value` + `onChange`) or uncontrolled (`defaultValue`).
  - exports: `QuantityStepperSize`, `QuantityStepperProps`, `QuantityStepper`
  - styles: yes
- **RatingSummary** — RatingSummary — product rating overview: a big average, a star display, the total count, and a per-star distribution breakdown (5→1).
  - exports: `RatingSummaryProps`, `RatingSummary`
  - styles: yes
- **ReviewCard** — ReviewCard — a single customer review: avatar/name, verified badge, star rating, date, title, body and a helpful (count + thumbs) action.
  - exports: `ReviewCardProps`, `ReviewCard`
  - styles: yes
- **ShippingTracker** — ShippingTracker — a commerce-flavored stepper of order states (ordered → shipped → out for delivery → delivered) with dates and icons.
  - exports: `ShippingStageStatus`, `ShippingStage`, `ShippingTrackerProps`, `ShippingTracker`
  - styles: yes
- **SizeSelector** — SizeSelector — a radiogroup of selectable size options (S/M/L/XL or numeric). Supports per-option disabled / out-of-stock and full keyboard navigation.
  - exports: `SizeSelectorSize`, `SizeOption`, `SizeSelectorProps`, `SizeSelector`
  - styles: yes
- **StockBadge** — StockBadge — availability indicator with a tone + dot. Covers in-stock, low-stock ("n left"), out-of-stock and preorder.
  - exports: `StockStatus`, `StockBadgeSize`, `StockBadgeProps`, `StockBadge`
  - styles: yes
- **VariantSelector** — VariantSelector — a generic labeled, single-select group of pill options for an arbitrary product attribute (material, style, finish…). Compose several to cover a product's variants. Renders as a radiogroup with keyboard support.
  - exports: `VariantSelectorSize`, `VariantOption`, `VariantSelectorProps`, `VariantSelector`
  - styles: yes
- **WishlistButton** — WishlistButton — a heart toggle with an animated fill. Works controlled (`active` + `onChange`) or uncontrolled (`defaultActive`). Optionally shows a saved count. Exposes `aria-pressed` for state.
  - exports: `WishlistButtonSize`, `WishlistButtonProps`, `WishlistButton`
  - styles: yes

## cursor

_9 components._

- **BlobCursor** — A gooey metaball cursor: several circles trail the pointer and merge through an SVG `feGaussianBlur` + `feColorMatrix` goo filter. The blob squishes along its direction of travel proportional to velocity.
  - exports: `BlobCursorProps`, `BlobCursor`
  - styles: yes
- **CursorFollower** — A single soft blob that lags behind the pointer with a spring simulation. Lightweight, decorative pointer companion for dark or playful sections.
  - exports: `CursorFollowerProps`, `CursorFollower`
  - styles: yes
- **CursorGlow** — A soft, tintable glowing light that follows the pointer. Uses an additive (`screen`) blend mode by default so it reads as light over dark sections. Pointer events pass through.
  - exports: `CursorGlowProps`, `CursorGlow`
  - styles: yes
- **CustomCursor** — Classic two-element custom cursor: a crisp dot pinned to the pointer plus a larger ring that eases toward it. The ring grows when hovering interactive or `[data-cursor]` elements.
  - exports: `CustomCursorProps`, `CustomCursor`
  - styles: yes
- **EmojiCursor** — Emits a configurable set of emojis/characters that spray and float from the pointer as it moves, then drift, spin, and fade under gravity. Uses a fixed particle pool driven by a single rAF loop.
  - exports: `EmojiCursorProps`, `EmojiCursor`
  - styles: yes
- **MagneticCursor** — A cursor ring that magnetizes onto nearby `[data-magnetic]` targets: when the pointer is close, the ring morphs to the target's bounding box (size, radius, position) instead of following the pointer freely.
  - exports: `MagneticCursorProps`, `MagneticCursor`
  - styles: yes
- **SpotlightCursor** — A dark overlay with a transparent radial "flashlight" hole that follows the pointer — a reveal-in-the-dark effect. Content behind the overlay shows through the hole. Pointer events pass through so the page stays interactive.
  - exports: `SpotlightCursorProps`, `SpotlightCursor`
  - styles: yes
- **TextCursor** — A label/text that trails the cursor (e.g. "view", "drag"). By default the puck is hidden and reveals when the pointer enters a `[data-cursor-text]` target, morphing to that element's `data-cursor-text` value.
  - exports: `TextCursorProps`, `TextCursor`
  - styles: yes
- **TrailCursor** — A fading trail of dots that follow the pointer. Particles are spawned as the pointer moves and decay every frame; a fixed pool is reused so no DOM nodes are created or destroyed during animation.
  - exports: `TrailCursorProps`, `TrailCursor`
  - styles: yes

## data-display

_44 components._

- **AnimatedCounter**
  - exports: `AnimatedCounterProps`, `AnimatedCounter`
  - styles: yes
- **Avatar**
  - exports: `AvatarSize`, `AvatarShape`, `AvatarStatus`, `AvatarProps`, `Avatar`
  - styles: yes
- **AvatarGroup**
  - exports: `AvatarGroupProps`, `AvatarGroup`
  - styles: yes
- **CalendarHeatmap**
  - exports: `HeatmapValue`, `CalendarHeatmapTone`, `CalendarHeatmapProps`, `CalendarHeatmap`
  - styles: yes
- **CodeBlock**
  - exports: `CodeBlockProps`, `CodeBlock`
  - styles: yes
- **CodeDiff** — CodeDiff — unified or split diff view with line numbers, +/- gutter and token-tinted add/del backgrounds. Pass pre-parsed `lines`, or `oldText` + `newText` for a built-in LCS line diff.
  - exports: `DiffLineType`, `DiffLine`, `CodeDiffProps`, `CodeDiff`
  - styles: yes
- **ColorPalette** — ColorPalette — displays color swatches with labels + values, with copy-on-click (clipboard guarded for SSR). Pass `swatches` for a flat grid or `scales` for grouped token scales.
  - exports: `PaletteSwatch`, `PaletteScale`, `ColorPaletteProps`, `ColorPalette`
  - styles: yes
- **ComparisonTable** — ComparisonTable — a feature-comparison matrix (features × plans). Supports boolean (check / cross), text, or node cell values, a highlighted column, and a sticky first column + header.
  - exports: `ComparisonAlign`, `ComparisonValue`, `ComparisonColumn`, `ComparisonRow`, `ComparisonTableProps`, `ComparisonTable`
  - styles: yes
- **DataList** — DataList — a definition-style record viewer. Renders `{label, value}` pairs as aligned rows, ideal for detail panels, with optional per-row copy support.
  - exports: `DataListItem`, `DataListProps`, `DataList`
  - styles: yes
- **DataTable**
  - exports: `DataTableAlign`, `SortDirection`, `DataTableColumn`, `DataTableProps`, `DataTable`
  - styles: yes
- **DescriptionList**
  - exports: `DescriptionListLayout`, `DescriptionListProps`, `DescriptionList`, `DescriptionListItemProps`, `DescriptionListItem`
  - styles: yes
- **EditableTable**
  - exports: `EditableTableAlign`, `EditableCellType`, `EditableSelectOption`, `EditableTableColumn`, `EditableTableProps`, `EditableTable`
  - styles: yes
- **EmptyState**
  - exports: `EmptyStateProps`, `EmptyState`
  - styles: yes
- **EventCalendar**
  - exports: `CalendarEvent`, `EventCalendarProps`, `EventCalendar`
  - styles: yes
- **Feed**
  - exports: `FeedProps`, `Feed`, `CommentProps`, `Comment`
  - styles: yes
- **FileTree** — FileTree — a read-only, file-explorer-flavored tree with folder/file icons, expand/collapse, active-file highlight, and `onSelect`. Distinct from a generic disclosure TreeView.
  - exports: `FileTreeNode`, `FileTreeProps`, `FileTree`
  - styles: yes
- **GanttChart**
  - exports: `GanttTask`, `GanttScale`, `GanttChartProps`, `GanttChart`
  - styles: yes
- **HeatGrid** — A single matrix cell. `value` may be null for an absent/blank cell.
  - exports: `HeatGridCell`, `HeatGridProps`, `HeatGrid`
  - styles: yes
- **InfiniteScroll**
  - exports: `InfiniteScrollProps`, `InfiniteScroll`
  - styles: yes
- **JsonViewer**
  - exports: `JsonViewerProps`, `JsonViewer`
  - styles: yes
- **KanbanBoard** — KanbanBoard — a display-focused board of columns and cards. Reordering and drag are intentionally out of scope (handled by the dnd category); this keeps a clean, read-only column layout with optional click handling.
  - exports: `KanbanColumnTone`, `KanbanCard`, `KanbanColumn`, `KanbanBoardProps`, `KanbanBoard`
  - styles: yes
- **KbdGroup** — Render a key combo such as `Cmd + K` as a group of styled keys. Self-contained — renders its own `<kbd>` keys (does not depend on the typography `Kbd` component) while matching the same visual treatment.
  - exports: `KbdGroupSize`, `KbdGroupProps`, `KbdGroup`, `Shortcut`, `ShortcutProps`
  - styles: yes
- **Leaderboard**
  - exports: `LeaderboardTrend`, `LeaderboardEntry`, `LeaderboardProps`, `Leaderboard`
  - styles: yes
- **List**
  - exports: `ListVariant`, `ListProps`, `List`, `ListItemProps`, `ListItem`
  - styles: yes
- **MetricCard**
  - exports: `MetricTrendDirection`, `MetricTone`, `MetricTrend`, `MetricCardProps`, `MetricCard`
  - styles: yes
- **OrgChart** — Pure subtree-width layout: each leaf occupies one slot; a parent is centered over its visible children. No DOM measurement — SSR-safe.
  - exports: `OrgChartNode`, `OrgChartProps`, `OrgChart`
  - styles: yes
- **PivotTable**
  - exports: `PivotAggregator`, `PivotTableProps`, `PivotTable`
  - styles: yes
- **ProgressRing**
  - exports: `ProgressRingTone`, `ProgressRingProps`, `ProgressRing`
  - styles: yes
- **ProgressTracker**
  - exports: `TrackerStepStatus`, `TrackerStep`, `ProgressTrackerProps`, `ProgressTracker`
  - styles: yes
- **PropertyList**
  - exports: `PropertyItem`, `PropertyGroup`, `PropertyListProps`, `PropertyList`
  - styles: yes
- **RatingDisplay**
  - exports: `RatingDisplaySize`, `RatingDisplayProps`, `RatingDisplay`
  - styles: yes
- **ResizableTable**
  - exports: `ResizableTableAlign`, `ResizableTableColumn`, `ResizableTableProps`, `ResizableTable`
  - styles: yes
- **Stat**
  - exports: `StatTrendDirection`, `StatTrend`, `StatProps`, `Stat`
  - styles: yes
- **StatGroup** — Lays out multiple Stat-like cards in a responsive row with optional dividers. Wrap any number of `<Stat>` (or compatible) children.
  - exports: `StatGroupProps`, `StatGroup`
  - styles: yes
- **Statistic**
  - exports: `StatisticTrend`, `StatisticProps`, `Statistic`
  - styles: yes
- **Table**
  - exports: `TableAlign`, `TableProps`, `TableSectionProps`, `TableHead`, `TableBody`, `TableFoot`, `TableRowProps`, `TableRow`, `TableCellProps`, `TableCell`, `TableHeaderCellProps`, `TableHeaderCell`
  - styles: yes
- **TableToolbar**
  - exports: `TableToolbarColumnToggle`, `TableToolbarProps`, `TableToolbar`
  - styles: yes
- **Tag**
  - exports: `TagTone`, `TagVariant`, `TagSize`, `TagProps`, `Tag`
  - styles: yes
- **TagCloud**
  - exports: `TagCloudItem`, `TagCloudProps`, `TagCloud`
  - styles: yes
- **Timeline**
  - exports: `TimelineTone`, `TimelineProps`, `Timeline`, `TimelineItemProps`, `TimelineItem`
  - styles: yes
- **TreeDiagram** — Tidy layout: leaves are packed along the cross axis; parents center over their children. `depth` drives the main axis. Pure — no DOM measuring.
  - exports: `TreeDiagramNode`, `TreeDiagramOrientation`, `TreeDiagramProps`, `TreeDiagram`
  - styles: yes
- **TreeTable**
  - exports: `TreeTableAlign`, `TreeSortDirection`, `TreeTableColumn`, `TreeTableProps`, `TreeTable`
  - styles: yes
- **TrendBadge**
  - exports: `TrendDirection`, `TrendBadgeSize`, `TrendBadgeProps`, `TrendBadge`
  - styles: yes
- **VirtualList** — VirtualList — windowed list that renders only the rows inside (and just around) the viewport, so very large datasets stay performant.
  - exports: `VirtualListProps`, `VirtualList`
  - styles: yes

## devices

_8 components._

- **BrowserFrame** — BrowserFrame — realistic browser-window chrome wrapping a content slot. Traffic-light dots, optional tabs, navigation toolbar and an address bar. Adapts to light/dark via the `chrome` prop and semantic tokens.
  - exports: `BrowserTab`, `BrowserFrameProps`, `BrowserFrame`
  - styles: yes
- **CodeWindow** — CodeWindow — code-editor window with a title bar (filename + language dot), optional tab strip and an optional line-number gutter wrapping a code slot. Line numbers are derived from the rendered text content.
  - exports: `CodeTab`, `CodeWindowProps`, `CodeWindow`
  - styles: yes
- **DeviceShowcase** — DeviceShowcase — presents a device on a subtle gradient/spotlight pedestal with optional float, hover-tilt and a mirrored reflection. All motion is CSS-driven and respects reduced-motion via duration tokens.
  - exports: `DeviceShowcaseProps`, `DeviceShowcase`
  - styles: yes
- **MacbookFrame** — MacbookFrame — laptop mockup with a notched screen bezel and a base wedge. Content fills the screen. Optional `tilt` adds a subtle 3D perspective.
  - exports: `MacbookFrameProps`, `MacbookFrame`
  - styles: yes
- **PhoneFrame** — PhoneFrame — modern smartphone mockup: rounded bezel, dynamic island/notch, side buttons and an optional status bar. Content fills the screen.
  - exports: `PhoneFrameProps`, `PhoneFrame`
  - styles: yes
- **TabletFrame** — TabletFrame — tablet mockup with an even bezel and a front camera dot. Content fills the screen. Supports portrait / landscape orientation.
  - exports: `TabletFrameProps`, `TabletFrame`
  - styles: yes
- **TerminalWindow** — TerminalWindow — terminal/console mockup with traffic lights, a title bar and a monospace body. Optional `lines` render a color-coded prompt with a CSS-only blinking caret (SSR-safe, respects reduced-motion).
  - exports: `TerminalLine`, `TerminalWindowProps`, `TerminalWindow`
  - styles: yes
- **WindowFrame** — WindowFrame — generic OS app-window chrome: a title bar with window controls, an optional sidebar slot and a content slot. Static "resizable" look.
  - exports: `WindowFrameProps`, `WindowFrame`
  - styles: yes

## disclosure

_9 components._

- **Accordion**
  - exports: `AccordionType`, `AccordionVariant`, `AccordionProps`, `Accordion`, `AccordionItemProps`, `AccordionItem`, `AccordionTriggerProps`, `AccordionTrigger`, `AccordionHeader`, `AccordionHeaderProps`, `AccordionPanelProps`, `AccordionPanel`
  - styles: yes
- **Collapsible**
  - exports: `CollapsibleProps`, `Collapsible`, `CollapsibleTriggerProps`, `CollapsibleTrigger`, `CollapsibleContentProps`, `CollapsibleContent`
  - styles: yes
- **DetailsGroup** — A thin grouping wrapper around native `<details>`/`<summary>` elements styled to the design system. With `exclusive`, only one item stays open at a time.
  - exports: `DetailsItemProps`, `DetailsItem`, `DetailsGroupProps`, `DetailsGroup`
  - styles: yes
- **Disclosure**
  - exports: `DisclosureRenderProps`, `DisclosureProps`, `Disclosure`
  - styles: yes
- **ExpandableCard** — A card whose body expands/collapses via an always-visible header toggle. The body animates open with a grid-rows height transition. Good for settings rows and list items.
  - exports: `ExpandableCardProps`, `ExpandableCard`
  - styles: yes
- **FAQ** — FAQ — a self-contained question/answer list built on the accordion pattern (it does not import Accordion). Smoothly animates answer height and emits schema-friendly markup (itemscope FAQPage / Question / Answer).
  - exports: `FAQItem`, `FAQMode`, `FAQProps`, `FAQ`
  - styles: yes
- **ReadMore** — ReadMore — truncates content to a fixed number of lines using pure CSS line-clamp (SSR-safe, no measuring) with a smooth toggle to reveal the rest.
  - exports: `ReadMoreProps`, `ReadMore`
  - styles: yes
- **Spoiler** — Spoiler — obscures sensitive content (spoilers, secrets) behind a blur or solid block, revealing it on click. The whole region is an accessible button while hidden so it is keyboard operable.
  - exports: `SpoilerMode`, `SpoilerProps`, `Spoiler`
  - styles: yes
- **TreeView**
  - exports: `TreeNode`, `TreeViewProps`, `TreeView`
  - styles: yes

## dnd

_9 components._

- **Draggable** — Draggable — wraps a child to make it draggable via the native HTML5 DnD API. Carries a `data` payload to `Droppable` targets and exposes drag lifecycle callbacks. SSR-safe (all logic runs inside event handlers).
  - exports: `DraggableProps`, `Draggable`
  - styles: yes
- **DragHandle** — DragHandle — a styled grip affordance used to initiate a drag. Renders a `<button>` so it is keyboard-focusable and announces itself; consumers wire pointer/keyboard handlers (Sortable does this automatically via `useHandle`).
  - exports: `DragHandleProps`, `DragHandle`
  - styles: yes
- **DragScroll** — DragScroll — a scroll container you can click-and-drag to pan horizontally and/or vertically, with optional kinetic momentum on release. Uses pointer events with listeners attached only while a drag is active, all torn down on unmount. SSR-safe (no DOM access at module scope or render).
  - exports: `DragScrollProps`, `DragScroll`
  - styles: yes
- **Droppable** — Droppable — a drop-target zone that highlights while a compatible item is dragged over it and invokes `onDrop` with the payload on release. Handles the dragenter/over/leave dance (including `preventDefault` so the browser permits the drop) and supports an `accept` predicate. SSR-safe.
  - exports: `DroppableProps`, `Droppable`
  - styles: yes
- **DropZone** — DropZone — a generic, dashed-outline content/file drop target styled like an upload affordance. Highlights on drag-over and accepts both `Draggable` payloads and OS file drops. Distinct from file-specific inputs: this is a neutral content surface. SSR-safe.
  - exports: `DropZoneProps`, `DropZone`
  - styles: yes
- **KanbanDnd** — Generic `forwardRef` wrapper. The cast preserves the generic card type for consumers while satisfying `forwardRef`'s non-generic signature.
  - exports: `KanbanCardData`, `KanbanColumnData`, `KanbanLocation`, `KanbanCardRenderProps`, `KanbanDndProps`, `KanbanDnd`
  - styles: yes
- **Sortable** — Generic `forwardRef` wrapper. The cast preserves the generic item type for consumers while satisfying `forwardRef`'s non-generic signature.
  - exports: `SortableItemData`, `SortableRenderProps`, `SortableProps`, `Sortable`
  - styles: yes
- **SortableGrid** — Generic `forwardRef` wrapper. The cast preserves the generic item type for consumers while satisfying `forwardRef`'s non-generic signature.
  - exports: `SortableGridItemData`, `SortableGridRenderProps`, `SortableGridProps`, `SortableGrid`
  - styles: yes
- **SortableTabs** — SortableTabs — a horizontal tab strip whose tabs can be reordered by dragging, combined with standard tab selection. Click or use arrow keys to activate a tab; press-drag a tab to reorder it (a moving gap shows the drop target). Keyboard reorder: focus a tab, hold no modifier and use Ctrl/Cmd+Arrow to move it. SSR-safe; pointer listeners cleaned up.
  - exports: `SortableTabItem`, `SortableTabsProps`, `SortableTabs`
  - styles: yes

## feedback

_26 components._

- **Alert**
  - exports: `AlertTone`, `AlertVariant`, `AlertProps`, `Alert`
  - styles: yes
- **Badge**
  - exports: `BadgeTone`, `BadgeVariant`, `BadgeSize`, `BadgeProps`, `Badge`
  - styles: yes
- **Banner** — Banner — full-width, edge-to-edge announcement bar for the top of a page. Slimmer and flatter than Alert; not boxed.
  - exports: `BannerTone`, `BannerProps`, `Banner`
  - styles: yes
- **Callout** — Callout — a boxed, docs-style highlighted note with a left accent border, icon, title and rich body. Content-focused; not dismissible.
  - exports: `CalloutTone`, `CalloutProps`, `Callout`
  - styles: yes
- **Confetti** — Imperative handle exposed via ref.
  - exports: `ConfettiHandle`, `ConfettiOrigin`, `ConfettiProps`, `Confetti`
  - styles: yes
- **Countdown** — Countdown — a live timer counting down to a target time or a duration from mount. SSR-safe: the target timestamp is resolved once in an effect (not during render), the interval lives in an effect with cleanup, and the initial render shows a stable zero-state so server and client markup match.
  - exports: `CountdownSize`, `CountdownProps`, `Countdown`
  - styles: yes
- **DotsLoader**
  - exports: `DotsLoaderSize`, `DotsLoaderTone`, `DotsLoaderVariant`, `DotsLoaderProps`, `DotsLoader`
  - styles: yes
- **Indicator** — Indicator — a small status dot / pulse. Use standalone, or wrap a child (avatar, icon, button) to position a notification dot or count on its corner.
  - exports: `IndicatorTone`, `IndicatorPlacement`, `IndicatorProps`, `Indicator`
  - styles: yes
- **InlineMessage** — InlineMessage — compact inline validation/help text, e.g. under a form field. `danger` and `warning` tones announce assertively for accessibility.
  - exports: `InlineMessageTone`, `InlineMessageProps`, `InlineMessage`
  - styles: yes
- **LoadingBar** — LoadingBar — a thin top-of-page progress indicator with a smooth indeterminate "trickle" toward completion, like a route loader. SSR-safe. When `loading` flips false it completes and fades out before unmounting.
  - exports: `LoadingBarTone`, `LoadingBarProps`, `LoadingBar`
  - styles: yes
- **LoadingOverlay**
  - exports: `LoadingOverlayProps`, `LoadingOverlay`
  - styles: yes
- **Meter** — Meter — a labeled measurement bar for a quantitative value within a known range. Recolors based on low/high/optimum thresholds. For task completion use Progress instead.
  - exports: `MeterSize`, `MeterProps`, `Meter`
  - styles: yes
- **NotificationCard**
  - exports: `NotificationCardTone`, `NotificationCardProps`, `NotificationCard`
  - styles: yes
- **Progress**
  - exports: `ProgressTone`, `ProgressSize`, `ProgressProps`, `Progress`
  - styles: yes
- **ProgressCircle**
  - exports: `ProgressCircleTone`, `ProgressCircleProps`, `ProgressCircle`
  - styles: yes
- **ProgressList**
  - exports: `ProgressListStatus`, `ProgressListItem`, `ProgressListProps`, `ProgressList`
  - styles: yes
- **ProgressSteps** — ProgressSteps — a slim horizontal progress visualization: labeled step markers joined by a connecting bar that fills to the current step (or to an explicit percent). Distinct from a navigation `Steps` / wizard.
  - exports: `ProgressStepsTone`, `ProgressStepsSize`, `ProgressStep`, `ProgressStepsProps`, `ProgressSteps`
  - styles: yes
- **RatingFeedback** — RatingFeedback — a compact feedback widget: a star / emoji / thumbs scale with an optional comment box and a submit action. Controlled or uncontrolled via `rating` / `onRatingChange`.
  - exports: `RatingFeedbackType`, `RatingFeedbackValue`, `RatingFeedbackProps`, `RatingFeedback`
  - styles: yes
- **Result** — Result — a centered, full-section status screen (success / error / 404 / 403 / 500 / info / warning) with an icon, title, subtitle, an actions slot, and optional extra content. Announced via `role="status"`.
  - exports: `ResultStatus`, `ResultProps`, `Result`
  - styles: yes
- **Ribbon** — Ribbon — a diagonal corner ribbon overlaying a container. Wraps its children with `position: relative` and pins a tone-colored banner across one corner.
  - exports: `RibbonTone`, `RibbonPlacement`, `RibbonProps`, `Ribbon`
  - styles: yes
- **Skeleton**
  - exports: `SkeletonVariant`, `SkeletonProps`, `Skeleton`
  - styles: yes
- **SkeletonPresets**
  - exports: `SkeletonTextProps`, `SkeletonText`, `SkeletonAvatarSize`, `SkeletonAvatarShape`, `SkeletonAvatarProps`, `SkeletonAvatar`, `SkeletonCardProps`, `SkeletonCard`
  - styles: yes
- **Spinner**
  - exports: `SpinnerSize`, `SpinnerProps`, `Spinner`
  - styles: yes
- **StatusIndicator** — StatusIndicator — a labelled status: colored dot + text describing a state. Distinct from `Indicator`, which is a positioned corner notification dot.
  - exports: `StatusIndicatorStatus`, `StatusIndicatorSize`, `StatusIndicatorProps`, `StatusIndicator`
  - styles: yes
- **Tooltip** — Pure-CSS positioned tooltip. Wraps a trigger and reveals `content` on hover or keyboard focus. The bubble is always rendered (so it can be referenced via `aria-describedby`) and toggled with CSS visibility — no portals, no JS positioning, SSR-safe.
  - exports: `TooltipPlacement`, `TooltipProps`, `Tooltip`
  - styles: yes
- **Watermark** — Watermark — a repeating diagonal text or image overlay tiled across its children (for branding / security / "draft" stamping). SSR-safe: the tile is a pure-string SVG data URI built during render, no window/canvas access.
  - exports: `WatermarkProps`, `Watermark`
  - styles: yes

## flair

_47 components._

- **AnimatedModal** — A trigger button that morphs/expands into a centered modal dialog with a shared-element feel (scales + fades up from the trigger position) over a blurred backdrop. Rendered through a portal (SSR-safe), with focus trapping, Escape-to-close, scroll lock, and `role="dialog"`/`aria-modal`. Works controlled (`open` + `onOpenChange`) or uncontrolled.
  - exports: `AnimatedModalProps`, `AnimatedModal`
  - styles: yes
- **AnimatedTooltipGroup** — A row of overlapping avatars; hovering one pops a spring tooltip with name and title that subtly rotates and slides toward the cursor's horizontal position over the avatar (the Aceternity animated tooltip). Pointer X is mapped to a small rotation + translate written to CSS variables. Each avatar is a focusable button; the tooltip also reveals on keyboard focus and is wired via `aria-describedby`. SSR-safe.
  - exports: `AnimatedTooltipItem`, `AnimatedTooltipGroupProps`, `AnimatedTooltipGroup`
  - styles: yes
- **BookmarkButton** — A bookmark/save toggle whose ribbon performs a fold/flip fill on save: the outline folds in 3D and reveals a filled ribbon. Controlled via `saved` or uncontrolled via `defaultSaved`. Real `<button>` with `aria-pressed`. The flip resolves instantly under reduced motion (handled in CSS).
  - exports: `BookmarkButtonProps`, `BookmarkButton`
  - styles: yes
- **BorderTrail** — A self-looping border trail: a small glowing segment continuously travels around the element's border path (top → right → bottom → left), distinct from a cursor-following border. Implemented with a conic-gradient mask animated via CSS, so it needs no JS and is GPU-friendly. Reduced motion freezes the trail.
  - exports: `BorderTrailProps`, `BorderTrail`
  - styles: yes
- **CanvasRevealCard** — A card that, on hover, fades in an animated dot-matrix canvas underneath the front content — dots twinkle (random opacity flicker) like the Aceternity canvas-reveal effect. The reveal layer (optional `reveal` content) sits above the dots. SSR-safe: the canvas + requestAnimationFrame loop only start in an effect and are fully torn down on unmount. The loop runs only while hovered, and is skipped entirely under reduced motion (a static dot grid is drawn instead).
  - exports: `CanvasRevealCardProps`, `CanvasRevealCard`
  - styles: yes
- **CardStack** — A stack of cards that cycles: the front card animates away (lifts, fades) and the next rises to the top, looping. Cards behind are offset and scaled for depth. Auto-advances on an interval (pausable by hover) and on click. The stack is a `role="group"`; advancing is also a focusable button for keyboard users. Under reduced motion the transition is instant.
  - exports: `CardStackItem`, `CardStackProps`, `CardStack`
  - styles: yes
- **CheckboxMorph** — A checkbox whose box springs and fills while a checkmark draws itself, with a little radial burst on check. Burst spokes are DOM spans on even angles via per-piece CSS vars, removed after the animation through a tracked timer (cleared on unmount). Controlled via `checked` or uncontrolled via `defaultChecked`. `role="checkbox"` with `aria-checked`. Space/Enter toggle. No spring/burst under reduced motion.
  - exports: `CheckboxMorphProps`, `CheckboxMorph`
  - styles: yes
- **CometCard** — A card that tilts toward the cursor in 3D while a bright comet/sheen streak sweeps across its surface following the pointer, backed by a soft floating drop-shadow that shifts with the tilt. Pointer math runs inline and writes CSS variables; reduced motion disables the tilt and streak.
  - exports: `CometCardProps`, `CometCard`
  - styles: yes
- **ConfettiButton** — A button that pops a small confetti burst from its center on click. Each burst is a set of DOM spans flung outward along randomized angles/distances (driven by per-piece CSS variables) and gravity-dropped; the whole burst is removed after its animation via a tracked timer, with all timers cleared on unmount. Real `<button>`. Under reduced motion no confetti is emitted.
  - exports: `ConfettiButtonProps`, `ConfettiButton`
  - styles: yes
- **CopyButton** — A button whose icon morphs from a "copy" glyph to an animated checkmark on a successful clipboard write, showing a "Copied!" tooltip. Copy is guarded: it tries `navigator.clipboard.writeText` and falls back to a temporary textarea + `execCommand`, all SSR-safe. The success state auto-resets via a cleaned-up timer. Real `<button>` with `aria-live` feedback. The check draw-in is instant under reduced motion.
  - exports: `CopyButtonProps`, `CopyButton`
  - styles: yes
- **DayNightToggle** — A delightful day↔night theme-style toggle: a sun slides across and morphs into a moon while clouds slide out and stars fade in. Purely visual/decorative — it reports its state through `onChange` but does not drive the real theme. Controlled via `night` or uncontrolled via `defaultNight`. Real `<button>` with `aria-pressed`. Transitions collapse to instant under reduced motion (handled in CSS).
  - exports: `DayNightToggleProps`, `DayNightToggle`
  - styles: yes
- **DirectionAwareHover** — An image/card whose overlay slides in from the edge the cursor actually entered (and slides back out toward the edge it leaves). The enter direction is computed from the pointer position relative to the element center using the aspect-corrected quadrant, then written to a `data-direction` attribute that CSS maps to the right slide transform. SSR-safe (geometry read only inside pointer handlers).
  - exports: `DirectionAwareHoverProps`, `DirectionAwareHover`
  - styles: yes
- **DraggableCardBody** — A card you can grab and throw: drag releases with velocity inertia, the card glides, tilts in the direction of motion, then springs back to its origin. Physics run in a rAF loop driven by pointer velocity; all transforms write to a child element so `...rest`/ref stay on the wrapper. Under reduced motion the card stays put (no throw/spring) but remains grabbable for visual feedback.
  - exports: `DraggableCardBodyProps`, `DraggableCardBody`
  - styles: yes
- **ElasticSlider** — A slider that scales up while interacting and stretches/squishes elastically when you drag past either end (the Family/elastic feel). Pointer drags are tracked on the document; overshoot beyond the track edges is mapped to an `--nova-es-overflow` variable that drives a non-uniform scale. Controlled or uncontrolled. Accessible: `role="slider"` with aria value props, full keyboard support (arrows / Home / End / PageUp-Down). Elastic squish is dropped under reduced motion (functionality intact).
  - exports: `ElasticSliderProps`, `ElasticSlider`
  - styles: yes
- **EvervaultCard** — An "encrypt on hover" card (Aceternity Evervault): a dense field of random characters constantly re-scrambles and is revealed only through a radial mask that follows the cursor, over a brand gradient. The front content sits above. SSR-safe — the scramble interval runs in an effect and is cleared on unmount / when not hovered. Under reduced motion the characters do not re-scramble.
  - exports: `EvervaultCardProps`, `EvervaultCard`
  - styles: yes
- **FlipCard** — A card that flips in 3D to reveal a back face, on hover or click. The two faces are absolutely stacked with `backface-visibility: hidden`; the inner wrapper rotates around the chosen axis. With the click trigger the card is a real toggle button (keyboard operable, `aria-pressed`). Under reduced motion the flip is instant via tokens.
  - exports: `FlipCardProps`, `FlipCard`
  - styles: yes
- **FocusCards** — A grid of cards where hovering (or focusing) one keeps it crisp and slightly scaled up while the others blur and dim (the Aceternity focus-cards effect). Each card is a focusable button so keyboard users get the same emphasis. Pure CSS for the blur/dim; a tiny state hook tracks the active index so focus and hover share one source of truth. Under reduced motion only the dim remains (no blur transition jump).
  - exports: `FocusCardItem`, `FocusCardsProps`, `FocusCards`
  - styles: yes
- **GlareCard** — A card that tilts toward the cursor while a bright glare highlight and an iridescent holographic sheen sweep across its surface following the pointer (the Aceternity glare card). Pointer position drives CSS variables for both the rotation and the glare/sheen gradient positions. Under reduced motion, tilt is disabled and the glare stays centered & dimmed.
  - exports: `GlareCardProps`, `GlareCard`
  - styles: yes
- **GlowingEffectBorder** — A wrapper whose border emits a bright glow that tracks the cursor around the card's edge (the Aceternity "glowing effect"). The pointer angle relative to the card center drives a conic gradient masked to just the border ring. SSR-safe (angle computed in pointer handler), and under reduced motion the glow stays put / hidden instead of chasing the cursor.
  - exports: `GlowingEffectBorderProps`, `GlowingEffectBorder`
  - styles: yes
- **GooeyButton** — A button with a gooey/liquid hover morph: small blobs grow from the surface and merge with the button body through an SVG gooey filter (feGaussianBlur + feColorMatrix alpha threshold), giving a viscous metaball look. A scoped inline <svg> filter keeps it self-contained and SSR-safe; the filter id is unique per instance via useId. Real `<button>`. Blob growth pauses under reduced motion (filter stays inert).
  - exports: `GooeyButtonProps`, `GooeyButton`
  - styles: yes
- **GradientCard** — A card sitting on an animated multi-stop gradient backdrop that drifts and shifts hue on hover (the "background gradient" card). The gradient lives in a blurred ::before layer behind a solid inner panel so content stays crisp and readable while the colorful glow animates underneath. The drift animation pauses under reduced motion.
  - exports: `GradientCardProps`, `GradientCard`
  - styles: yes
- **HoverBorderGradient** — A pill control wrapped in a continuously rotating conic gradient border that intensifies and speeds up on hover. The rotating gradient is a masked ring (border-box minus content-box) so only the rim shows; the interior is a solid token surface. CSS `@property` animates the conic angle with zero JS. Animation is fully CSS-driven and pauses under reduced motion.
  - exports: `HoverBorderGradientProps`, `HoverBorderGradient`
  - styles: yes
- **HoverRevealText** — A list of rows where hovering one floats its preview image near the cursor (the "following image on hover" link effect). The single shared preview element tracks the pointer and swaps its source to the active row's image. Rows render as links when `href` is given (keyboard-focusable). The preview is decorative (`aria-hidden`). Under reduced motion the image still appears but does not glide.
  - exports: `HoverRevealItem`, `HoverRevealTextProps`, `HoverRevealText`
  - styles: yes
- **InfiniteMovingCards** — A row of cards that scrolls infinitely and seamlessly: the list is duplicated and translated a full track-width, so the loop has no visible seam. Supports direction, speed presets (or a raw duration), pause-on-hover and edge fades. Pure CSS animation (SSR-safe, no measurement). The duplicate copy is hidden from assistive tech. Under reduced motion the scroll is paused.
  - exports: `InfiniteMovingCardsItem`, `InfiniteMovingCardsProps`, `InfiniteMovingCards`
  - styles: yes
- **Lens** — A circular magnifying lens that follows the cursor over its children, showing a zoomed copy of the content under the glass. Pointer position is written to CSS variables and the zoomed layer is a duplicate of the content scaled + translated so the same point stays under the cursor. Purely visual. SSR-safe (all reads happen inside pointer handlers).
  - exports: `LensProps`, `Lens`
  - styles: yes
- **LikeButton** — A heart toggle that fills and bursts particles on like, with a satisfying scale pop and an optional count that bumps. Particles are DOM spans flung along even angles via per-piece CSS vars and removed after the burst through a tracked timer (cleared on unmount). Controlled via `liked` or uncontrolled via `defaultLiked`. Real `<button>` with `aria-pressed`. No burst/pop under reduced motion.
  - exports: `LikeButtonProps`, `LikeButton`
  - styles: yes
- **LiquidToggle** — A switch whose thumb is a gooey liquid blob: as it slides it stretches toward its destination and snaps back, achieved with a leading "drip" element and an SVG gooey filter (feGaussianBlur + feColorMatrix alpha threshold). The filter id is unique per instance via useId, keeping it self-contained and SSR-safe. `role="switch"` with `aria-checked`. Space/Enter toggle. Reduced motion removes the stretch (handled in CSS).
  - exports: `LiquidToggleProps`, `LiquidToggle`
  - styles: yes
- **MagneticButton** — A control that is magnetically pulled toward the cursor while hovered and springs back to rest on leave. Movement is integrated each animation frame toward a pointer-derived target, so it eases naturally rather than snapping. Real `<button>` (keyboard/focus accessible). Under reduced motion the magnet is disabled and the button stays put.
  - exports: `MagneticButtonProps`, `MagneticButton`
  - styles: yes
- **MovingBorderButton** — A button with a bright light streak that continuously travels around its rounded border (the Aceternity moving border). The streak is a rotating conic-gradient layer masked to a thin rim; an inner solid panel holds the content, so the glow appears to orbit the edge. Real `<button>`. The orbit animation pauses under reduced motion.
  - exports: `MovingBorderButtonProps`, `MovingBorderButton`
  - styles: yes
- **MultiStepLoader** — A vertical checklist loader (Aceternity multi-step loader): steps advance automatically on a timer, each transitioning spinner → check as it completes while the active step is highlighted. Optionally loops. Honors reduced motion by swapping the spinner for a static dot.
  - exports: `MultiStepLoaderProps`, `MultiStepLoader`
  - styles: yes
- **ParallaxTiltImage** — An image card with multi-layer parallax tilt: the card rotates toward the cursor in 3D while the background image and the foreground content translate along Z by different amounts, so the foreground "lifts" more than the image for a layered depth effect. Pointer position drives rotation + per-layer shift through CSS variables. Under reduced motion the tilt/parallax is disabled and the card stays flat.
  - exports: `ParallaxTiltImageProps`, `ParallaxTiltImage`
  - styles: yes
- **PinContainer** — A 3D pin card (Aceternity-style): on hover the card tilts back in perspective and lifts while a floating "pin" label tag rises above it, backed by a perspective grid that recedes into the distance. Pure CSS transforms — no JS. Renders an `<a>` when `href` is given (so it's keyboard-focusable), otherwise a `<div>`. Under reduced motion the tilt/lift is suppressed.
  - exports: `PinContainerProps`, `PinContainer`
  - styles: yes
- **PowerButton** — An IoT-style power button: a power glyph inside a ring that lights up and glows when on, with a subtle press depress. Controlled via `on` or uncontrolled via `defaultOn`. Real `<button>` with `aria-pressed`. The glow/transition is instant under reduced motion (handled in CSS).
  - exports: `PowerButtonProps`, `PowerButton`
  - styles: yes
- **PulsatingButton** — An attention CTA that emits a soft glow ring pulsing outward from behind the button on a loop. The ring is a pseudo-element scaling up while fading out, driven by a pure CSS animation (disabled under reduced motion via tokens). Real `<button>` — keyboard and focus accessible.
  - exports: `PulsatingButtonProps`, `PulsatingButton`
  - styles: yes
- **RippleButton** — A button that spawns a Material-style ripple expanding from the exact click point and fading out. Each ripple is a DOM span sized to cover the button from the click origin; it self-removes after the animation via a tracked timeout, and any pending timers are cleared on unmount (no leaks). Real `<button>`. Under reduced motion no ripple is spawned.
  - exports: `RippleButtonProps`, `RippleButton`
  - styles: yes
- **SaveButton** — A stateful save button that animates idle → saving (spinner) → saved (a check that draws in). Either drive it with a controlled `status`, or pass an async `onSave`: the button shows the spinner while the promise is pending, morphs to the check on resolve, then returns to idle after `resetDelay`. The pending timer is cleared on unmount and a mounted-guard avoids state updates after teardown. Real `<button>`, disabled while saving. Spinner/draw are instant under reduced motion (handled in CSS).
  - exports: `SaveStatus`, `SaveButtonProps`, `SaveButton`
  - styles: yes
- **ScratchToReveal** — A canvas scratch-off surface: drag across the coating to erase it (a `destination-out` brush), revealing the children beneath. Cleared area is sampled periodically; when it passes `threshold` the coating auto-fades and `onComplete` fires once. SSR-safe (canvas painted in an effect, guarded), with pointer-capture drag and full listener/animation cleanup on unmount.
  - exports: `ScratchToRevealProps`, `ScratchToReveal`
  - styles: yes
- **ShimmerButton** — A premium CTA button with a sheen that continuously sweeps across its surface and a soft outer glow that intensifies on hover. The shimmer is a masked, rotating conic highlight running on a pure CSS animation (paused under reduced motion via tokens). Real `<button>` — keyboard and focus accessible.
  - exports: `ShimmerButtonProps`, `ShimmerButton`
  - styles: yes
- **SpotlightCard** — A card with a radial spotlight that follows the cursor inside it (the Vercel/Next.js hover effect), with an optional border highlight that tracks the same point. Pointer position is written to CSS variables; the glow fades in/out via opacity so there is no work when the pointer is away. Purely a visual effect — non-interactive content can sit inside. SSR-safe.
  - exports: `SpotlightCardProps`, `SpotlightCard`
  - styles: yes
- **StarToggle** — A star favorite toggle that spins and fills on activation while popping a ring of sparkles. Sparkles are DOM spans placed on even angles via per-piece CSS vars and removed after the pop through a tracked timer (cleared on unmount). Controlled via `favorited` or uncontrolled via `defaultFavorited`. Real `<button>` with `aria-pressed`. No spin/sparkles under reduced motion.
  - exports: `StarToggleProps`, `StarToggle`
  - styles: yes
- **StickyNoteCard** — A draggable sticky note that can be tossed around inside its positioned container, with a peeling corner curl and a soft drop shadow that lifts while dragging. Position is held on the element via translate; dragging is bounded to the offset parent when `bounded`. SSR-safe (pointer logic in handlers), releases pointer capture, and respects reduced motion by dropping the lift transition.
  - exports: `StickyNoteColor`, `StickyNoteCardProps`, `StickyNoteCard`
  - styles: yes
- **SubscribeButton** — An animated subscribe/follow button that morphs its label and color on subscribe while a check pops in, and reverses cleanly on unsubscribe. The check-pop animation flag is cleared via a tracked timer on unmount. Controlled via `subscribed` or uncontrolled via `defaultSubscribed`. Real `<button>` with `aria-pressed`. The pop is skipped under reduced motion; the state still changes instantly.
  - exports: `SubscribeButtonProps`, `SubscribeButton`
  - styles: yes
- **SwipeCards** — A Tinder-style deck: drag or fling the top card left/right to dismiss it, revealing the next. Pointer drag applies translation + rotation; releasing past the threshold flings the card off-screen, otherwise it springs back. Arrow keys (← / →) provide a keyboard fallback. SSR-safe (pointer logic in effects/handlers), cleans up pointer capture, and under reduced motion the fling is instant.
  - exports: `SwipeDirection`, `SwipeCardItem`, `SwipeCardsProps`, `SwipeCards`
  - styles: yes
- **ThreeDButton** — A chunky 3D push-button with real depth: a colored top face sits above a darker "edge" so the button reads as physically raised. Pressing (pointer or keyboard activation) translates the top face down into its edge for a satisfying tactile click, with the shadow collapsing. A real `<button>` — fully keyboard operable and focusable. Under reduced motion the depress is instant.
  - exports: `ThreeDButtonProps`, `ThreeDButton`
  - styles: yes
- **ThumbsToggle** — A combined thumbs up / thumbs down rating control with single-select: each thumb bounces and fills with a semantic color when chosen; clicking the active one clears the selection. Controlled via `value` or uncontrolled via `defaultValue`. A `role="group"` of two real `<button>`s; the active one carries `aria-pressed`. Bounce is suppressed under reduced motion (handled in CSS).
  - exports: `ThumbsValue`, `ThumbsToggleProps`, `ThumbsToggle`
  - styles: yes
- **TiltCard** — A 3D card that tilts in perspective toward the cursor, with a light highlight and optional glare layer that follow the pointer. The transform is written to CSS custom properties so the eased transition is handled in CSS. Under reduced motion the tilt and glare are disabled.
  - exports: `TiltCardProps`, `TiltCard`
  - styles: yes
- **WobbleCard** — A card that elastically wobbles toward the cursor with a spring ease, while its inner content drifts a little further for a layered parallax. Position is driven by CSS variables and the elastic spring easing token, so releasing the pointer lets it overshoot gently back to center. Under reduced motion the wobble and parallax are disabled.
  - exports: `WobbleCardProps`, `WobbleCard`
  - styles: yes

## fx

_32 components._

- **AnimatedGradientBorder** — A card wrapped in a slowly rotating conic-gradient border glow. The gradient is painted onto the border ring and the whole conic angle is animated via an `@property`-driven custom property for buttery rotation, with an optional blurred outer halo. SSR-safe (pure CSS). Border + glow layers are aria-hidden. Freezes on reduced-motion.
  - exports: `AnimatedGradientBorderProps`, `AnimatedGradientBorder`
  - styles: yes
- **AnimatedGridPattern** — An SVG grid where random cells continuously light up and fade out, then jump to fresh positions (the Magic UI animated grid). Lit-cell count, max opacity, dwell time, cell size and edge fade are configurable. SSR-safe: deterministic initial cells for stable first paint; reshuffling runs in an effect/timer with cleanup. Freezes on reduced-motion. Decorative — aria-hidden.
  - exports: `AnimatedGridPatternProps`, `AnimatedGridPattern`
  - styles: yes
- **AuroraBackground** — Animated flowing aurora behind content — overlapping conic/radial gradient blobs that slowly drift and rotate for a premium, ambient backdrop. SSR-safe (pure CSS animation, no JS). Decorative layer is aria-hidden. Honors reduced-motion via the global token freeze.
  - exports: `AuroraBackgroundProps`, `AuroraBackground`
  - styles: yes
- **AuroraRibbons** — Flowing aurora ribbons — a few wavy gradient bands that undulate horizontally across the background (distinct from AuroraBackground's soft blobs). Built with animated SVG paths and gradient strokes. SSR-safe (no window access; ids via useId). Undulation freezes under reduced-motion. Decorative — aria-hidden.
  - exports: `AuroraRibbonsProps`, `AuroraRibbons`
  - styles: yes
- **BackgroundBoxes** — A large grid of cells that light up with a brand tint as the cursor hovers over them — the Aceternity "background boxes". Pure CSS hover (no JS), with an optional skewed 3D plane. SSR-safe (no window access). Decorative — aria-hidden. The hover transition is neutralized under reduced-motion via tokens; the tint still appears instantly.
  - exports: `BackgroundBoxesProps`, `BackgroundBoxes`
  - styles: yes
- **Beams** — Diagonal light beams that slowly sweep across a section — a few angled soft-gradient bars drifting for a premium hero backdrop. Beam count, tilt angle, color and speed are configurable. SSR-safe: beam parameters derived deterministically per render (stable markup), pure CSS animation. Freezes on reduced-motion. Decorative layer aria-hidden.
  - exports: `BeamsProps`, `Beams`
  - styles: yes
- **BeamsCollision** — Beams fall from the top and burst into a splash of sparks when they hit the bottom line — the Magic UI "background beams with collision". Rendered to a canvas via requestAnimationFrame. SSR-safe: all canvas/window access lives in an effect with full cleanup (rAF cancelled, ResizeObserver disconnected). Freezes (static frame) under reduced-motion. Decorative — aria-hidden.
  - exports: `BeamsCollisionProps`, `BeamsCollision`
  - styles: yes
- **BorderBeam** — A focused light beam that travels around an element's border, drawn with a conic-gradient masked to just the border ring (mask composite). Wraps children and overlays the animated ring on top. SSR-safe (pure CSS via the `offset-path`/conic technique). Decorative ring is aria-hidden. Freezes on reduced-motion.
  - exports: `BorderBeamProps`, `BorderBeam`
  - styles: yes
- **CanvasRevealEffect** — A dot-matrix canvas that animates in as a diagonal wave — each dot fades and scales up. Usable as a hover-reveal layer (drop into a hover target and toggle `animate`). Colors and speed are configurable. SSR-safe: all canvas/window access lives in an effect with full cleanup (rAF cancelled, ResizeObserver disconnected). Renders a static fully-revealed frame under reduced-motion. Decorative — aria-hidden.
  - exports: `CanvasRevealEffectProps`, `CanvasRevealEffect`
  - styles: yes
- **ConcentricRings** — Concentric ring outlines that pulse outward from a point and fade — a sonar / radar ping. Ring count, color, cadence and origin are configurable; the rings are evenly phase-offset so a new one launches at a steady beat. SSR-safe (pure CSS, no window access). Rings freeze under reduced-motion via CSS. Decorative — aria-hidden.
  - exports: `ConcentricRingsProps`, `ConcentricRings`
  - styles: yes
- **DotPattern** — SVG dot-grid background. Optional radial fade mask plus an interactive glow that tracks the pointer via a CSS variable updated in an rAF-throttled pointermove handler. SSR-safe (no window access during render; listener is inline on the element). Decorative — aria-hidden. Glow uses a mask so it only reveals the dots.
  - exports: `DotPatternProps`, `DotPattern`
  - styles: yes
- **DottedGlobe** — A rotating sphere of dots — a lightweight "globe of points". Dots are placed on a lat/long grid and positioned with CSS 3D transforms (rotateY → rotateX → translateZ) inside a `preserve-3d` stage that slowly spins. No three.js, no canvas. SSR-safe: the dot layout is computed deterministically during render (no window access). The spin freezes under reduced-motion via CSS. Decorative — aria-hidden.
  - exports: `DottedGlobeProps`, `DottedGlobe`
  - styles: yes
- **FloatingShapes** — Soft geometric shapes (circles, triangles, blobs) that drift and parallax slowly across the background, each on its own loop. Tintable, with a configurable count. Positions are deterministic so SSR and first paint match. SSR-safe (seeded layout, no window access). Drift freezes under reduced-motion via CSS. Decorative — aria-hidden.
  - exports: `FloatingShapesProps`, `FloatingShapes`
  - styles: yes
- **GlowingStars** — A field of twinkling stars that brighten and bloom near the cursor. Stars are lightweight DOM dots with staggered twinkle animations; a brand-tinted glow masked to the cursor reveals an extra-bright copy of the field on hover. SSR-safe (positions derived deterministically; pointer tracking in an rAF-throttled handler). Star layers are aria-hidden; content renders above.
  - exports: `GlowingStarsProps`, `GlowingStars`
  - styles: yes
- **GradientAnimation** — A smoothly animated multi-color mesh gradient — five large radial blobs that drift, scale and orbit independently behind content for a premium ambient backdrop. Speed, blur, the five blob colors and the base background are configurable; accepts content. SSR-safe (pure CSS animation, no JS). Freezes on reduced-motion. Decorative layer aria-hidden.
  - exports: `GradientAnimationProps`, `GradientAnimation`
  - styles: yes
- **GridPattern** — SVG graph-paper grid background. Configurable cell size, stroke dash, optional radial fade mask, and a set of randomly-placed squares that gently pulse. SSR-safe: random highlight positions are derived deterministically per render with a memo so the markup is stable. Decorative — aria-hidden.
  - exports: `GridPatternProps`, `GridPattern`
  - styles: yes
- **HexPattern** — A hexagonal-tile background pattern built from a tiling SVG pattern of pointy hex outlines, with an optional radial edge fade and a configurable number of randomly placed hexes that gently twinkle. Hover lights individual cells. SSR-safe: twinkle positions derived deterministically per render (stable markup), pure CSS. Twinkle freezes on reduced-motion. Decorative — aria-hidden.
  - exports: `HexPatternProps`, `HexPattern`
  - styles: yes
- **InteractiveGridPattern** — A grid of SVG cells that highlight individually as the cursor moves over them (pointer-tracked). Subtle hover fill on the hovered cell; cell size, column / row count and edge fade are configurable. SSR-safe: hover state is React-driven (no direct window access), pure CSS for the visuals. No animation to gate, but the fill transition honors the global reduced-motion token freeze. Decorative — aria-hidden.
  - exports: `InteractiveGridPatternProps`, `InteractiveGridPattern`
  - styles: yes
- **LampGlow** — A focused conic lamp glow that fans out from a thin top bar — a pure-background variant of the Aceternity "lamp" (distinct from a heading-paired LampHeader). Content renders above the glow via a slot. SSR-safe (no window access). The glow layer is aria-hidden; the breathing animation is disabled under reduced-motion via CSS.
  - exports: `LampGlowProps`, `LampGlow`
  - styles: yes
- **Meteors** — Animated meteor shower — diagonal streaks that fall across the container, each with a glowing head and a fading trailing tail. Counts, speed, and angle are configurable; positions/delays are derived deterministically per render. SSR-safe (CSS animation, stable markup). Decorative — aria-hidden.
  - exports: `MeteorsProps`, `Meteors`
  - styles: yes
- **NoiseOverlay** — A subtle film-grain/noise texture overlay generated entirely from an inline SVG `feTurbulence` data-URI — no image assets. Sits above content as a decorative, non-interactive layer. Optional flicker animates the grain. SSR-safe (the data-URI is built during render, no browser APIs). Decorative — aria-hidden, pointer-events none. Flicker freezes on reduced-motion.
  - exports: `NoiseOverlayProps`, `NoiseOverlay`
  - styles: yes
- **Particles** — Floating particle field rendered to a canvas via requestAnimationFrame. Dots drift continuously and gently part around the cursor. Quantity, color, speed, size and cursor reach are configurable. SSR-safe: all canvas/window access lives in effects with full cleanup (rAF cancelled, listeners + ResizeObserver removed). Freezes on reduced-motion. Decorative — aria-hidden.
  - exports: `ParticlesProps`, `Particles`
  - styles: yes
- **PlasmaField** — A flowing plasma / lava-lamp field built from layered animated radial gradient blobs that drift and a hue-rotate that slowly shifts the palette. Content renders above the field via a slot. SSR-safe (pure CSS, no window access). The drift + hue animations are disabled under reduced-motion via CSS; the field freezes to a static gradient.
  - exports: `PlasmaFieldProps`, `PlasmaField`
  - styles: yes
- **RetroGrid** — Perspective scrolling retro/synthwave grid floor. A tilted plane of glowing lines scrolls toward the horizon using CSS 3D transforms, fading out into the distance for that Tron/Outrun look. SSR-safe (pure CSS). Decorative — aria-hidden. Freezes on reduced-motion.
  - exports: `RetroGridProps`, `RetroGrid`
  - styles: yes
- **RippleBackground** — Concentric expanding ripple circles that emanate from the center on a loop (the Magic UI ripple). Each ring is larger and fainter; ring count, base size, spacing, color and loop speed are configurable. Accepts centered content. SSR-safe (pure CSS animation, stable markup). Freezes on reduced-motion. Decorative layer aria-hidden.
  - exports: `RippleBackgroundProps`, `RippleBackground`
  - styles: yes
- **ShineBorder** — An animated shining gradient border around a card. A moving multi-stop gradient is painted only on the border ring (mask-composite), creating a continuous sheen. Wraps children; the inner surface stays themeable. SSR-safe (pure CSS). Border layer is aria-hidden. Freezes on reduced-motion.
  - exports: `ShineBorderProps`, `ShineBorder`
  - styles: yes
- **ShootingStars** — Occasional shooting stars that streak diagonally across an (optionally starry) night sky. Each streak has a glowing head and a fading tail; spawn frequency, colors and speed are configurable. SSR-safe: all timers live in an effect with full cleanup. Freezes (no streaks) on reduced-motion. Decorative — aria-hidden.
  - exports: `ShootingStarsProps`, `ShootingStars`
  - styles: yes
- **SpotlightCorners** — Two angled spotlight cones beaming in from the top corners to illuminate a dark hero — the Aceternity multi-spotlight. Content renders above the cones via a slot. SSR-safe (pure CSS, no window access). The sweep animation is disabled under reduced-motion via CSS (cones stay lit, static). Cone layer is aria-hidden.
  - exports: `SpotlightCornersProps`, `SpotlightCorners`
  - styles: yes
- **SpotlightGlow** — A radial spotlight glow that follows the cursor across a section — the classic "Linear hero" effect. The glow position is pushed to CSS variables in an rAF-throttled pointermove handler; content renders above it. SSR-safe (no window access during render). Glow layer is aria-hidden and fades out when the pointer leaves.
  - exports: `SpotlightGlowProps`, `SpotlightGlow`
  - styles: yes
- **StarsBackground** — A still or twinkling starfield made of several parallax depth layers of dots, for dark hero backgrounds. Nearer layers have larger, brighter, faster-pulsing stars. Density, layer count, twinkle and color are configurable. SSR-safe: star positions derived deterministically per render (stable markup), pure CSS animation. Twinkle freezes on reduced-motion. Decorative — aria-hidden.
  - exports: `StarsBackgroundProps`, `StarsBackground`
  - styles: yes
- **Vortex** — A swirling particle vortex rendered to a canvas via requestAnimationFrame: dots orbit a central point, each at its own radius and angular speed, fading with depth. Density, speed, color and particle size are configurable; accepts centered content. SSR-safe: all canvas/window access lives in effects with full cleanup (rAF cancelled, ResizeObserver removed). Renders a single static frame on reduced-motion. Decorative layer aria-hidden.
  - exports: `VortexProps`, `Vortex`
  - styles: yes
- **WarpBackground** — A perspective "warp tunnel" — beams fly toward the viewer from all four edges along a 3D-perspective grid, with a content slot in the center. Beam count per side, speed, color and perspective depth are configurable. SSR-safe: beam offsets derived deterministically per render (stable markup), pure CSS animation. Freezes on reduced-motion. Decorative layer aria-hidden.
  - exports: `WarpBackgroundProps`, `WarpBackground`
  - styles: yes

## gamification

_12 components._

- **AchievementBadge**
  - exports: `AchievementTier`, `AchievementBadgeProps`, `AchievementBadge`
  - styles: yes
- **ComboMeter**
  - exports: `ComboMeterProps`, `ComboMeter`
  - styles: yes
- **DailyRewardRow**
  - exports: `DailyReward`, `DailyRewardRowProps`, `DailyRewardRow`
  - styles: yes
- **LevelBadge**
  - exports: `LevelTier`, `LevelBadgeProps`, `LevelBadge`
  - styles: yes
- **ProgressMilestones**
  - exports: `Milestone`, `ProgressMilestonesProps`, `ProgressMilestones`
  - styles: yes
- **ProgressQuest**
  - exports: `QuestObjective`, `ProgressQuestProps`, `ProgressQuest`
  - styles: yes
- **RankBadge**
  - exports: `Rank`, `RankBadgeProps`, `RankBadge`
  - styles: yes
- **RewardCard**
  - exports: `RewardCardProps`, `RewardCard`
  - styles: yes
- **ScoreCounter**
  - exports: `ScoreCounterProps`, `ScoreCounter`
  - styles: yes
- **SpinWheel**
  - exports: `SpinWheelSegment`, `SpinWheelProps`, `SpinWheel`
  - styles: yes
- **StreakCounter**
  - exports: `StreakCounterProps`, `StreakCounter`
  - styles: yes
- **XpProgressBar**
  - exports: `XpTone`, `XpProgressBarProps`, `XpProgressBar`
  - styles: yes

## inputs

_57 components._

- **AutosizeInput**
  - exports: `AutosizeInputSize`, `AutosizeInputProps`, `AutosizeInput`
  - styles: yes
- **Button**
  - exports: `ButtonVariant`, `ButtonTone`, `ButtonSize`, `ButtonProps`, `Button`
  - styles: yes
- **Calendar** — Strip time → midnight local.
  - exports: `CalendarSize`, `CalendarProps`, `Calendar`
  - styles: yes
- **CardForm**
  - exports: `CardFormSize`, `CardFormValue`, `CardFormProps`, `CardForm`
  - styles: yes
- **Cascader**
  - exports: `CascaderSize`, `CascaderOption`, `CascaderProps`, `Cascader`
  - styles: yes
- **Checkbox**
  - exports: `CheckboxSize`, `CheckboxProps`, `Checkbox`
  - styles: yes
- **ColorPicker** — A palette of color swatches with optional custom-color trigger.
  - exports: `ColorPickerSize`, `ColorSwatchProps`, `ColorSwatch`, `ColorPickerProps`, `ColorPicker`
  - styles: yes
- **ColorWheel**
  - exports: `ColorWheelSize`, `ColorWheelProps`, `ColorWheel`
  - styles: yes
- **Combobox**
  - exports: `ComboboxSize`, `ComboboxOption`, `ComboboxProps`, `Combobox`
  - styles: yes
- **CreditCardInput** — Detect a card brand from its leading digits.
  - exports: `CreditCardInputSize`, `CardBrand`, `CreditCardChange`, `CreditCardInputProps`, `detectCardBrand`, `luhnValid`, `formatCardNumber`, `parseCardNumber`, `CreditCardInput`
  - styles: yes
- **CurrencyInput** — Resolve the currency symbol for a locale/currency pair.
  - exports: `CurrencyInputSize`, `CurrencyInputProps`, `CurrencyInput`
  - styles: yes
- **CvvInput**
  - exports: `CvvInputSize`, `CvvChange`, `CvvInputProps`, `CvvInput`
  - styles: yes
- **DatePicker**
  - exports: `DatePickerSize`, `DatePickerProps`, `DatePicker`
  - styles: yes
- **DateRangePicker** — A start/end date range. Either side may be `null` while selecting.
  - exports: `DateRangePickerSize`, `DateRange`, `DateRangePickerProps`, `DateRangePicker`
  - styles: yes
- **DurationSlider** — Format a minute count as `Xh Ym` (omitting empty parts).
  - exports: `DurationSliderSize`, `DurationSliderProps`, `formatMinutes`, `DurationSlider`
  - styles: yes
- **Editable**
  - exports: `EditableSize`, `EditableProps`, `Editable`
  - styles: yes
- **EmojiPicker**
  - exports: `EmojiPickerSize`, `EmojiEntry`, `EmojiCategory`, `EmojiPickerProps`, `EmojiPicker`
  - styles: yes
- **EmojiRating**
  - exports: `EmojiRatingSize`, `EmojiRatingItem`, `EmojiRatingProps`, `EmojiRating`
  - styles: yes
- **ExpiryInput** — Normalize free input into up to 4 expiry digits. A leading month > 1 is padded (e.g. "5" -> "05") so the slash appears naturally.
  - exports: `ExpiryInputSize`, `ExpiryValue`, `ExpiryChange`, `ExpiryInputProps`, `parseExpiry`, `ExpiryInput`
  - styles: yes
- **FieldGroup** — Gap presets mapped onto the --nova-space-* scale.
  - exports: `FieldGroupOrientation`, `FieldGroupGap`, `FieldGroupProps`, `FieldGroup`
  - styles: yes
- **FileDropzone** — Match a file against an `accept` string (extensions + mime, incl. wildcards).
  - exports: `FileDropzoneSize`, `FileRejection`, `FileDropzoneProps`, `FileDropzone`
  - styles: yes
- **Form** — Lightweight form wrapper. Renders a `<form>`, prevents default on submit, runs validation, and exposes the form API via context + an optional render-prop.
  - exports: `FormValues`, `FormErrors`, `FieldOptions`, `FieldRegistration`, `FormApi`, `useFormContext`, `useForm`, `useField`, `FormProviderProps`, `FormProvider`, `FormProps`, `Form`
  - styles: yes
- **FormField**
  - exports: `FormFieldRenderProps`, `FormFieldProps`, `FormField`
  - styles: yes
- **GradientPicker**
  - exports: `GradientPickerSize`, `GradientStop`, `GradientPickerProps`, `GradientPicker`
  - styles: yes
- **IconPicker**
  - exports: `IconPickerSize`, `IconPickerItem`, `IconPickerProps`, `IconPicker`
  - styles: yes
- **ImageCardSelect**
  - exports: `ImageCardSelectSize`, `ImageCardOption`, `ImageCardSelectProps`, `ImageCardSelect`
  - styles: yes
- **Input**
  - exports: `InputSize`, `InputProps`, `Input`
  - styles: yes
- **Knob** — Arc sweep, in degrees, from min to max (gap at the bottom).
  - exports: `KnobSize`, `KnobProps`, `Knob`
  - styles: yes
- **LikertScale**
  - exports: `LikertOption`, `LikertScaleProps`, `LikertScale`
  - styles: yes
- **MaskedInput** — Apply a mask to an input string, returning the formatted display value and the raw (unformatted) characters that filled the token slots. The input is sanitized first: any character that can never satisfy an upcoming token (including the mask's own literals the user may have typed or that came from a previously-formatted value) is dropped, so re-feeding a formatted value is idempotent.
  - exports: `MaskedInputSize`, `MaskedInputChange`, `MaskedInputProps`, `applyMask`, `MaskedInput`
  - styles: yes
- **Mentions**
  - exports: `MentionsSize`, `MentionOption`, `MentionsProps`, `Mentions`
  - styles: yes
- **MultiSelect**
  - exports: `MultiSelectSize`, `MultiSelectOption`, `MultiSelectProps`, `MultiSelect`
  - styles: yes
- **NpsScale**
  - exports: `NpsScaleProps`, `NpsScale`
  - styles: yes
- **NumberInput**
  - exports: `NumberInputSize`, `NumberInputProps`, `NumberInput`
  - styles: yes
- **OtpResendTimer**
  - exports: `OtpResendTimerProps`, `OtpResendTimer`
  - styles: yes
- **PasswordInput** — Compute strength from length + character-class variety.
  - exports: `PasswordInputSize`, `PasswordStrength`, `PasswordStrengthResult`, `PasswordInputProps`, `computePasswordStrength`, `PasswordInput`
  - styles: yes
- **PhoneInput** — A small, dependency-free list of ~30 common countries.
  - exports: `PhoneInputSize`, `PhoneCountry`, `PhoneInputValue`, `PhoneInputProps`, `PHONE_COUNTRIES`, `PhoneInput`
  - styles: yes
- **PinInput**
  - exports: `PinInputSize`, `PinInputProps`, `PinInputRef`, `PinInput`
  - styles: yes
- **PostalCodeInput** — Light per-country postal rules. Unlisted countries accept any non-empty value.
  - exports: `PostalCodeInputSize`, `PostalCodeChange`, `PostalRule`, `PostalCodeInputProps`, `POSTAL_RULES`, `postalRuleFor`, `validatePostal`, `PostalCodeInput`
  - styles: yes
- **Radio**
  - exports: `RadioProps`, `Radio`
  - styles: yes
- **RangeSlider** — A `[min, max]` tuple.
  - exports: `RangeSliderSize`, `RangeValue`, `RangeSliderProps`, `RangeSlider`
  - styles: yes
- **Rating**
  - exports: `RatingSize`, `RatingProps`, `Rating`
  - styles: yes
- **RichTextEditor** — True once running in the browser (avoids SSR document access).
  - exports: `RichTextCommand`, `RichTextEditorProps`, `RichTextEditor`
  - styles: yes
- **SearchInput**
  - exports: `SearchInputSize`, `SearchInputProps`, `SearchInput`
  - styles: yes
- **SegmentedControl**
  - exports: `SegmentedControlSize`, `SegmentedControlOption`, `SegmentedControlProps`, `SegmentedControl`
  - styles: yes
- **Select**
  - exports: `SelectSize`, `SelectProps`, `Select`
  - styles: yes
- **SignaturePad**
  - exports: `SignaturePadHandle`, `SignaturePadProps`, `SignaturePad`
  - styles: yes
- **Slider**
  - exports: `SliderSize`, `SliderProps`, `Slider`
  - styles: yes
- **SliderMarks**
  - exports: `SliderMarksSize`, `SliderMark`, `SliderMarksProps`, `SliderMarks`
  - styles: yes
- **Switch**
  - exports: `SwitchSize`, `SwitchProps`, `Switch`
  - styles: yes
- **TagInput**
  - exports: `TagInputSize`, `TagInputProps`, `TagInput`
  - styles: yes
- **Textarea**
  - exports: `TextareaSize`, `TextareaProps`, `Textarea`
  - styles: yes
- **TimePicker** — A time value as minutes/seconds since midnight, 24h based.
  - exports: `TimePickerSize`, `TimeValue`, `TimePickerProps`, `TimePicker`
  - styles: yes
- **Toggle**
  - exports: `ToggleVariant`, `ToggleSize`, `ToggleProps`, `Toggle`
  - styles: yes
- **ToggleChipGroup**
  - exports: `ToggleChipGroupSize`, `ToggleChipOption`, `ToggleChipGroupProps`, `ToggleChipGroup`
  - styles: yes
- **ToggleGroup**
  - exports: `ToggleGroupItem`, `ToggleGroupSingleProps`, `ToggleGroupMultipleProps`, `ToggleGroupProps`, `ToggleGroup`
  - styles: yes
- **TransferList**
  - exports: `TransferListSize`, `TransferListItem`, `TransferListProps`, `TransferList`
  - styles: yes

## layout

_28 components._

- **Affix** — Affix — pins content to a viewport edge or corner with a configurable offset (a floating helper / back-to-top / chat launcher). Uses `position: fixed`, so it stays put while the page scrolls. SSR-safe: it only emits CSS positioning, no window/document access.
  - exports: `AffixPosition`, `AffixProps`, `Affix`
  - styles: yes
- **AppShell** — AppShell — responsive application layout built on CSS grid. Composes a sticky header, a collapsible left sidebar, an optional right aside, a footer and the main content region. Each region maps to a semantic landmark. On small screens the sidebar can be hidden off-canvas via the controlled `sidebarOpen` prop. SSR-safe (no `window`/`document` access).
  - exports: `AppShellProps`, `AppShell`
  - styles: yes
- **Bleed** — Bleed — an escape hatch that pulls its content past the parent's padding via token-scaled negative margins. Useful for full-bleed media inside a padded `Card` or `Container`. Per-side, with `all` / `x` / `y` shorthands.
  - exports: `BleedSpace`, `BleedProps`, `Bleed`
  - styles: yes
- **Box** — Box — the layout primitive. Renders a `div` by default, or any element via `as`. It is intentionally unstyled (a single reset-ish class) so every other layout component can build on top of it and pass `style`/`className` through.
  - exports: `BoxOwnProps`, `BoxProps`, `Box`
  - styles: yes
- **ButtonGroup** — ButtonGroup — a layout primitive that visually joins a row/column of buttons (segmented edges, shared borders). It does not import or render a Button; it styles whatever button-like children you place inside, so it works with native `<button>`, `<a>`, or any button component. Role `group`.
  - exports: `ButtonGroupOrientation`, `ButtonGroupSize`, `ButtonGroupProps`, `ButtonGroup`
  - styles: yes
- **Card** — Compound Card: `Card`, `Card.Header`, `Card.Body`, `Card.Footer`.
  - exports: `CardPadding`, `CardVariant`, `CardProps`, `CardSectionProps`, `Card`
  - styles: yes
- **CardGrid** — CardGrid — a responsive auto-fit grid tuned for cards. Set a `minCardWidth` and it lays out as many equal columns as fit, with consistent `gap` and optional equal heights. No masonry; rows align cleanly.
  - exports: `CardGridGap`, `CardGridProps`, `CardGrid`
  - styles: yes
- **Center** — Center — centers a single child on both axes. Pass `minHeight` to give the container height to center within (e.g. a full-viewport hero).
  - exports: `CenterProps`, `Center`
  - styles: yes
- **Cluster** — Cluster — a wrapping inline group with a consistent gap and alignment. The go-to primitive for tag lists, button rows, metadata, breadcrumb-like groups — anything that should flow inline and wrap gracefully.
  - exports: `ClusterGap`, `ClusterAlign`, `ClusterJustify`, `ClusterProps`, `Cluster`
  - styles: yes
- **Container** — Container — a horizontally-centered wrapper that caps content width and applies consistent gutters. The primary page-level layout boundary.
  - exports: `ContainerSize`, `ContainerProps`, `Container`
  - styles: yes
- **Divider** — Divider — a thematic separator. Renders a semantic separator with the correct `role`/`aria-orientation`; when a `label` is supplied it becomes a labelled group ("OR"-style divider).
  - exports: `DividerProps`, `Divider`
  - styles: yes
- **Flex** — Flex — a general-purpose flexbox container. More flexible than `Stack`: exposes every flex axis plus grow/shrink/basis helpers. Reach for `Stack` for simple one-direction spacing; reach for `Flex` when you need full control.
  - exports: `FlexGap`, `FlexDirection`, `FlexAlign`, `FlexJustify`, `FlexWrap`, `FlexProps`, `Flex`
  - styles: yes
- **Footer** — Footer — a site footer: an optional brand block, groups of link columns, and a bottom bar with copyright text and an optional social slot. Columns wrap responsively and collapse to a stack on small screens.
  - exports: `FooterLink`, `FooterColumn`, `FooterProps`, `Footer`
  - styles: yes
- **Frame** — Frame — a bordered, padded content frame with an optional label sitting on the border (fieldset-like grouping for arbitrary content). Not form-specific; use it to visually group any content. Renders semantically as a `fieldset`/`legend` so the label is associated with the group.
  - exports: `FrameVariant`, `FramePadding`, `FrameProps`, `Frame`
  - styles: yes
- **Grid** — Grid — a thin CSS Grid wrapper. Either a fixed column count, or a responsive `autoFit` mode that wraps columns based on `minColWidth`.
  - exports: `GridGap`, `GridProps`, `Grid`
  - styles: yes
- **Hero** — Hero — a large section header: optional eyebrow, a title + subtitle, an actions slot, and an optional media/visual that switches the layout to a two-column split. Supports `start`/`center` alignment, three sizes and an optional subtle or brand `gradient` background.
  - exports: `HeroAlign`, `HeroSize`, `HeroBackground`, `HeroProps`, `Hero`
  - styles: yes
- **Masonry** — Masonry — a CSS-columns masonry layout. Children flow top-to-bottom then wrap into the next column, packing items of varying height with no row gaps. `columns` accepts a fixed number or a responsive breakpoint map. Pure CSS — no measurement, fully SSR-safe.
  - exports: `MasonryGap`, `MasonryColumns`, `MasonryProps`, `Masonry`
  - styles: yes
- **PageHeader** — PageHeader — a page title block: optional breadcrumbs on top, an optional leading avatar/icon, a title + description, trailing actions, and an optional footer slot (typically tabs). Spacing comes entirely from tokens.
  - exports: `PageHeaderProps`, `PageHeader`
  - styles: yes
- **Panel** — Panel — a sectioned surface, lighter than `Card`. Renders an optional header (title + description + trailing actions), a body, and an optional footer. Good for dashboard sections. Variants: `subtle` (soft fill) | `outlined`.
  - exports: `PanelVariant`, `PanelProps`, `Panel`
  - styles: yes
- **Resizable** — Resizable — two panes split by a draggable divider. Drag with a pointer or nudge with arrow keys on the handle. Honours `min`/`max` bounds and reports changes via `onResize`. SSR-safe; pointer listeners live in effects and are cleaned up.
  - exports: `ResizableProps`, `Resizable`
  - styles: yes
- **ScrollArea** — ScrollArea — a styled scroll container with thin, themed scrollbars (webkit + Firefox `scrollbar-*`). Optionally renders fade masks at the scrollable edges. Pure CSS — no JS scroll listeners.
  - exports: `ScrollAreaAxis`, `ScrollAreaProps`, `ScrollArea`
  - styles: yes
- **Section** — Section — a semantic `<section>` with token-scaled vertical rhythm and an optional title/description header. Use it to break a page into evenly-spaced regions.
  - exports: `SectionSpace`, `SectionProps`, `Section`
  - styles: yes
- **SimpleGrid** — SimpleGrid — the common-case responsive grid. Give it `minChildWidth` for an auto-fit responsive grid, or `columns` for a fixed equal-width grid. For full control over spans and templates, use `Grid` instead.
  - exports: `SimpleGridGap`, `SimpleGridProps`, `SimpleGrid`
  - styles: yes
- **Spacer** — Spacer — an empty box used to add space between items in flex/grid layouts. Either a fixed `size` from the token scale, or a flexible spacer that expands to push neighbours apart.
  - exports: `SpacerSize`, `SpacerProps`, `Spacer`
  - styles: yes
- **Split** — Split — a clean static two-pane split with a configurable `ratio` and `gap`. Not a draggable resizer; just a predictable layout primitive. Panes stack when `direction="vertical"`.
  - exports: `SplitGap`, `SplitProps`, `Split`
  - styles: yes
- **Stack** — Stack — a one-dimensional flex container. Lays children out in a column (default) or row with a token-based `gap`. Use it for the vast majority of spacing needs instead of margins.
  - exports: `StackGap`, `StackAlign`, `StackJustify`, `StackProps`, `Stack`
  - styles: yes
- **Sticky** — Sticky — wraps content in `position: sticky` with a configurable edge, offset and z-index. When `trackStuck` is set it uses an IntersectionObserver (SSR-guarded) to flag the stuck state via `data-stuck` and `onStuckChange`.
  - exports: `StickyProps`, `Sticky`
  - styles: yes
- **Toolbar** — Toolbar — a horizontal (or vertical) strip of control groups with `start`/`center`/`end` alignment regions and optional wrapping. Carries `role="toolbar"`. With `keyboardNav`, arrow keys move focus between the enabled controls (Home/End jump to the ends).
  - exports: `ToolbarAlign`, `ToolbarProps`, `ToolbarGroupProps`, `Toolbar`, `ToolbarGroup`, `ToolbarSeparatorProps`, `ToolbarSeparator`
  - styles: yes

## loaders

_24 components._

- **AiThinkingLoader**
  - exports: `AiThinkingLoaderTone`, `AiThinkingLoaderSize`, `AiThinkingLoaderProps`, `AiThinkingLoader`
  - styles: yes
- **BatteryLoader**
  - exports: `BatteryLoaderTone`, `BatteryLoaderSize`, `BatteryLoaderProps`, `BatteryLoader`
  - styles: yes
- **BlobLoader**
  - exports: `BlobLoaderTone`, `BlobLoaderSize`, `BlobLoaderProps`, `BlobLoader`
  - styles: yes
- **BouncingBalls**
  - exports: `BouncingBallsTone`, `BouncingBallsSize`, `BouncingBallsProps`, `BouncingBalls`
  - styles: yes
- **ClockLoader**
  - exports: `ClockLoaderTone`, `ClockLoaderSize`, `ClockLoaderProps`, `ClockLoader`
  - styles: yes
- **CometLoader**
  - exports: `CometLoaderTone`, `CometLoaderSize`, `CometLoaderProps`, `CometLoader`
  - styles: yes
- **DnaLoader**
  - exports: `DnaLoaderTone`, `DnaLoaderSize`, `DnaLoaderProps`, `DnaLoader`
  - styles: yes
- **FlipBoxLoader**
  - exports: `FlipBoxLoaderTone`, `FlipBoxLoaderSize`, `FlipBoxLoaderProps`, `FlipBoxLoader`
  - styles: yes
- **GooeyLoader**
  - exports: `GooeyLoaderTone`, `GooeyLoaderSize`, `GooeyLoaderProps`, `GooeyLoader`
  - styles: yes
- **HeartbeatLoader** — ECG path drawn across a 100x40 viewBox: flat line with a single QRS spike.
  - exports: `HeartbeatLoaderTone`, `HeartbeatLoaderSize`, `HeartbeatLoaderProps`, `HeartbeatLoader`
  - styles: yes
- **InfinityLoader** — Lemniscate of Gerono scaled into the 0..100 / 0..50 viewBox.
  - exports: `InfinityLoaderTone`, `InfinityLoaderSize`, `InfinityLoaderProps`, `InfinityLoader`
  - styles: yes
- **MatrixRainLoader**
  - exports: `MatrixRainLoaderTone`, `MatrixRainLoaderSize`, `MatrixRainLoaderProps`, `MatrixRainLoader`
  - styles: yes
- **MorphingLoader**
  - exports: `MorphingLoaderTone`, `MorphingLoaderSize`, `MorphingLoaderProps`, `MorphingLoader`
  - styles: yes
- **OrbitLoader**
  - exports: `OrbitLoaderTone`, `OrbitLoaderSize`, `OrbitLoaderProps`, `OrbitLoader`
  - styles: yes
- **PacmanLoader**
  - exports: `PacmanLoaderTone`, `PacmanLoaderSize`, `PacmanLoaderProps`, `PacmanLoader`
  - styles: yes
- **ProgressDots**
  - exports: `ProgressDotsTone`, `ProgressDotsSize`, `ProgressDotsProps`, `ProgressDots`
  - styles: yes
- **ProgressOrb**
  - exports: `ProgressOrbTone`, `ProgressOrbSize`, `ProgressOrbProps`, `ProgressOrb`
  - styles: yes
- **PulseGridLoader** — Chebyshev distance from the center cell (1,1) — drives the ripple delay.
  - exports: `PulseGridLoaderTone`, `PulseGridLoaderSize`, `PulseGridLoaderProps`, `PulseGridLoader`
  - styles: yes
- **RingsLoader**
  - exports: `RingsLoaderTone`, `RingsLoaderSize`, `RingsLoaderProps`, `RingsLoader`
  - styles: yes
- **RippleLoader**
  - exports: `RippleLoaderTone`, `RippleLoaderSize`, `RippleLoaderProps`, `RippleLoader`
  - styles: yes
- **SpiralLoader**
  - exports: `SpiralLoaderTone`, `SpiralLoaderSize`, `SpiralLoaderProps`, `SpiralLoader`
  - styles: yes
- **SquareLoader**
  - exports: `SquareLoaderTone`, `SquareLoaderSize`, `SquareLoaderProps`, `SquareLoader`
  - styles: yes
- **TextShimmerLoader**
  - exports: `TextShimmerLoaderTone`, `TextShimmerLoaderSize`, `TextShimmerLoaderProps`, `TextShimmerLoader`
  - styles: yes
- **WaveLoader**
  - exports: `WaveLoaderTone`, `WaveLoaderSize`, `WaveLoaderProps`, `WaveLoader`
  - styles: yes

## media

_21 components._

- **AspectRatio** — Maintains a fixed aspect ratio for its content. Children are stretched to fill the box (absolutely positioned), so a single child like an `<img>` or `<iframe>` cleanly fills the frame.
  - exports: `AspectRatioProps`, `AspectRatio`
  - styles: yes
- **AudioPlayer** — Compact audio player with play/pause, seek bar, time readout, volume/mute and optional title/artist. SSR-safe — media access happens inside effects with proper listener cleanup.
  - exports: `AudioSource`, `AudioPlayerProps`, `AudioPlayer`
  - styles: yes
- **AudioWaveform** — Deterministic pseudo-random in [0,1) from an integer seed.
  - exports: `AudioWaveformProps`, `AudioWaveform`
  - styles: yes
- **AvatarUploader**
  - exports: `AvatarUploaderSize`, `AvatarUploaderProps`, `AvatarUploader`
  - styles: yes
- **BackgroundMedia** — Full-bleed background image or video with a token-based overlay tint and a content slot layered on top — ideal for heroes. Media is `object-fit: cover`. SSR-safe (the `<video>` element renders on the server without JS).
  - exports: `BackgroundMediaOverlay`, `BackgroundMediaProps`, `BackgroundMedia`
  - styles: yes
- **Carousel**
  - exports: `CarouselProps`, `Carousel`
  - styles: yes
- **Figure** — Semantic `<figure>` + `<figcaption>` wrapper with caption alignment control.
  - exports: `FigureAlign`, `FigureProps`, `Figure`
  - styles: yes
- **Gallery**
  - exports: `GalleryGap`, `GalleryLayout`, `GalleryItem`, `GalleryProps`, `Gallery`
  - styles: yes
- **Icon** — Inline SVG wrapper. Color inherits `currentColor`. Decorative by default; pass `label` for a meaningful, announced icon.
  - exports: `IconSize`, `IconProps`, `Icon`
  - styles: yes
- **Image**
  - exports: `ImageFit`, `ImageProps`, `Image`
  - styles: yes
- **ImageCompare** — Before/after image slider with a draggable divider. Operable by pointer (drag anywhere on the frame) and keyboard (arrows move the divider). SSR-safe: geometry reads happen only inside event handlers.
  - exports: `ImageCompareProps`, `ImageCompare`
  - styles: yes
- **ImageMasonry** — Responsive masonry image grid built on CSS multi-columns. Images load lazily; each item is independently clickable and may reveal a caption on hover.
  - exports: `ImageMasonryGap`, `ImageMasonryItem`, `ImageMasonryProps`, `ImageMasonry`
  - styles: yes
- **ImagePlaceholder** — Graceful image placeholder: shows a solid color / gradient block (blurhash- like) that crossfades to the real image once it loads. SSR-safe — the placeholder renders on the server and the crossfade is driven by the image's own `onLoad`. If the image is already cached/complete on mount, it's revealed immediately.
  - exports: `ImagePlaceholderProps`, `ImagePlaceholder`
  - styles: yes
- **ImageZoom**
  - exports: `ImageZoomMode`, `ImageZoomProps`, `ImageZoom`
  - styles: yes
- **MediaObject** — The classic "media object" layout: a fixed media block (image / icon / avatar) beside flexible heading + body content. Reversible and alignable.
  - exports: `MediaObjectAlign`, `MediaObjectGap`, `MediaObjectProps`, `MediaObject`
  - styles: yes
- **PlayButton** — Circular play / pause control intended to overlay media. Controlled via the `playing` prop; emits standard `onClick`. Optional pulsing ring.
  - exports: `PlayButtonSize`, `PlayButtonVariant`, `PlayButtonProps`, `PlayButton`
  - styles: yes
- **SeekBar** — Reusable media scrubber. Click or drag the track to seek; arrow keys seek by `step`. Optional buffered range and a time tooltip. role=slider, fully keyboard operable. SSR-safe — geometry is read only inside handlers.
  - exports: `SeekBarProps`, `SeekBar`
  - styles: yes
- **Thumbnail** — Small media tile: a rounded image frame with an optional play badge (video), duration badge and a selected/active state. Renders as a `<button>` so it is keyboard-operable by default.
  - exports: `ThumbnailSize`, `ThumbnailProps`, `Thumbnail`
  - styles: yes
- **VideoCard** — Video preview card: a thumbnail with play overlay + duration badge, plus title, channel/author and a meta line (views / date). Activatable via click or keyboard when `onActivate` is provided.
  - exports: `VideoCardProps`, `VideoCard`
  - styles: yes
- **VideoPlayer** — Custom-styled controls layered over a native `<video>`. Supports play/pause, seek with scrub, volume/mute, fullscreen, keyboard control, poster and multiple sources. SSR-safe — all media access happens inside effects.
  - exports: `VideoSource`, `VideoPlayerProps`, `VideoPlayer`
  - styles: yes
- **VolumeControl** — Reusable volume control: a mute toggle plus a slider (0–1). The building block for media players. Pointer-draggable and keyboard accessible (role=slider). SSR-safe — geometry is read only inside handlers.
  - exports: `VolumeControlSize`, `VolumeControlOrientation`, `VolumeControlProps`, `VolumeControl`
  - styles: yes

## motion

_9 components._

- **Collapse** — Animated expand/collapse for an `open` prop. Uses the CSS grid `0fr`/`1fr` trick so it works without measuring the DOM — fully SSR-safe and requires no JavaScript timing. Honors reduced motion via tokens + a media-query guard. The single direct child is the content wrapper; it must contain the content.
  - exports: `CollapseProps`, `Collapse`
  - styles: yes
- **Fade** — Fade in/out wrapper, built on the {@link Transition} primitive. Keeps the child mounted through the fade-out, then unmounts. SSR-safe and reduced-motion aware (inherited from `Transition`).
  - exports: `FadeProps`, `Fade`
  - styles: yes
- **Marquee** — Continuously scrolling marquee. The content is duplicated so the loop is seamless; one copy is hidden from assistive tech to avoid duplicate reads. Speed is converted to a duration based on the configured pixels-per-second. Pure CSS animation (SSR-safe, no measurement). Under reduced motion the animation is paused and the content sits statically.
  - exports: `MarqueeProps`, `Marquee`
  - styles: yes
- **Ripple** — Material-style click ripple wrapper. On pointer-down it spawns a ripple at the pointer coordinates sized to cover the element; each ripple removes itself once its animation ends (with a timeout safety net). Ripples are decorative and `aria-hidden`. Under reduced motion no ripples are spawned. Wrap any positioned/relative content; this renders a relatively-positioned inline-block host that clips the ripples.
  - exports: `RippleProps`, `Ripple`
  - styles: yes
- **Scale** — Scale + fade wrapper, built on {@link Transition}. The starting scale and the pivot are configurable via CSS custom properties.
  - exports: `ScaleProps`, `Scale`
  - styles: yes
- **Slide** — Slide + fade wrapper, built on {@link Transition}. The offset is expressed via a CSS custom property so a single transform handles all directions.
  - exports: `SlideDirection`, `SlideProps`, `Slide`
  - styles: yes
- **Stagger** — Animates its children in with a staggered, incremental delay. Each child is wrapped in an item element whose CSS animation is delayed by `index * gap`. Triggers on mount, or on an `in` prop when supplied. Reduced motion is handled by the stylesheet (animations disabled, items shown immediately).
  - exports: `StaggerDirection`, `StaggerProps`, `Stagger`
  - styles: yes
- **Transition** — Core mount/unmount transition primitive. Keeps its child mounted through the exit transition, then unmounts. Drives enter/exit via data attributes + class hooks; completion is detected with `transitionend` and a safety timer. SSR-safe: no window/document access during render; timers/listeners are registered in effects and cleaned up. Honors reduced motion by snapping.
  - exports: `TransitionPreset`, `TransitionStatus`, `TransitionClassNames`, `TransitionProps`, `Transition`
  - styles: yes
- **Typewriter** — Types out text, or cycles through an array of strings, optionally deleting and looping. Driven by `setTimeout` scheduled in an effect with full cleanup; the caret blinks via CSS. SSR-safe (no DOM access on the server) and snaps to the full (last) string under reduced motion.
  - exports: `TypewriterProps`, `Typewriter`
  - styles: yes

## navigation

_29 components._

- **Anchor** — Table-of-contents navigation with scroll-spy + smooth scrolling.
  - exports: `AnchorItem`, `AnchorProps`, `Anchor`
  - styles: yes
- **AnimatedHamburger** — AnimatedHamburger — a hamburger ↔ X toggle button. The three bars morph into a close icon with one of several styles. Fully controllable, sets `aria-expanded`, and respects reduced motion (bars snap instead of animating).
  - exports: `AnimatedHamburgerProps`, `AnimatedHamburger`
  - styles: yes
- **AppSwitcher** — AppSwitcher — a workspace/app switcher. The current item is a button that opens a popover list of apps with keyboard navigation and a selected check. Optionally exposes a "create new" action. SSR-safe.
  - exports: `AppSwitcherItem`, `AppSwitcherProps`, `AppSwitcher`
  - styles: yes
- **BottomNav**
  - exports: `BottomNavItem`, `BottomNavProps`, `BottomNav`
  - styles: yes
- **Breadcrumbs**
  - exports: `BreadcrumbItem`, `BreadcrumbsProps`, `Breadcrumbs`
  - styles: yes
- **CommandPalette**
  - exports: `CommandItem`, `CommandPaletteProps`, `CommandPalette`
  - styles: yes
- **Dock**
  - exports: `DockItem`, `DockSize`, `DockProps`, `Dock`
  - styles: yes
- **ExpandableSearch** — ExpandableSearch — an icon button that expands into a full search input on click/focus and collapses on blur (when empty) or Escape, with a smooth width animation. Calls `onSearch` on Enter / icon click. Collapsing keeps any typed value so it survives a re-open.
  - exports: `ExpandableSearchProps`, `ExpandableSearch`
  - styles: yes
- **FilterBar** — FilterBar — a horizontal toolbar that holds filter pills/chips, shows an active-filter count and a clear-all control. Sits above tables and lists.
  - exports: `FilterBarProps`, `FilterBar`, `FilterPillVariant`, `FilterPillProps`, `FilterPill`
  - styles: yes
- **FloatingActionMenu** — FloatingActionMenu — a "+" trigger that fans a radial arc of labelled action buttons on click. Distinct from a SpeedDial stack: actions are placed along an arc (transform translate from polar coords) with captions. Closes on outside-click, Escape, or after selecting an action.
  - exports: `FloatingActionMenuAction`, `FloatingActionMenuProps`, `FloatingActionMenu`
  - styles: yes
- **GooeyNav** — GooeyNav — a navigation bar whose active highlight is a gooey blob built with an SVG gooey filter. As the blob slides between items it stretches and merges with a trailing droplet, producing a liquid morph. Keyboard: arrow keys move between items, Home/End jump to the ends.
  - exports: `GooeyNavItem`, `GooeyNavProps`, `GooeyNav`
  - styles: yes
- **MegaMenu** — MegaMenu — a wide dropdown panel triggered by a top-level nav item. Holds multi-column sections of links plus an optional featured slot. Opens on hover and keyboard, closes on click-outside / Escape. SSR-safe.
  - exports: `MegaMenuProps`, `MegaMenu`, `MegaMenuTriggerProps`, `MegaMenuTrigger`, `MegaMenuPanelProps`, `MegaMenuPanel`, `MegaMenuSectionProps`, `MegaMenuSection`, `MegaMenuItemProps`, `MegaMenuItem`
  - styles: yes
- **Menu**
  - exports: `MenuPlacement`, `MenuProps`, `Menu`, `MenuTriggerProps`, `MenuTrigger`, `MenuListProps`, `MenuList`, `MenuItemProps`, `MenuItem`, `MenuSeparatorProps`, `MenuSeparator`
  - styles: yes
- **MorphMenu** — MorphMenu — a compact menu button that morphs/expands into a panel of links by animating height + opacity from the button's footprint. Closes on outside-click and Escape. Keyboard: Arrow keys move between items, Home/End jump, Escape closes and restores focus to the trigger.
  - exports: `MorphMenuItem`, `MorphMenuProps`, `MorphMenu`
  - styles: yes
- **Navbar** — Horizontal application bar. Pass nav links as `children` (typically a `<NavbarLink>` list); `brand` and `actions` fill the start/end slots.
  - exports: `NavbarProps`, `Navbar`, `NavbarLinkProps`, `NavbarLink`
  - styles: yes
- **NavList**
  - exports: `NavListProps`, `NavListSectionProps`, `NavListSection`, `NavListItemProps`, `NavListItem`, `NavList`
  - styles: yes
- **PageNav**
  - exports: `PageNavLink`, `PageNavProps`, `PageNav`
  - styles: yes
- **Pagination**
  - exports: `PaginationSize`, `PaginationProps`, `Pagination`
  - styles: yes
- **PillTabs** — PillTabs — tabs where the active background is a single sliding pill. The pill is positioned with a measured FLIP-style transform/scale (one shared element translates + resizes between tabs) so it glides smoothly. Keyboard: arrow keys move between tabs, Home/End jump to the ends.
  - exports: `PillTabsItem`, `PillTabsProps`, `PillTabs`
  - styles: yes
- **ScrollNav** — ScrollNav — a horizontally scrollable nav/tab strip. Left/right scroll buttons appear only when the content overflows, with optional edge fades. Keyboard accessible (arrow / Home / End) following the tablist pattern.
  - exports: `ScrollNavSize`, `ScrollNavItem`, `ScrollNavProps`, `ScrollNav`
  - styles: yes
- **ScrollToTop**
  - exports: `ScrollToTopPlacement`, `ScrollToTopProps`, `ScrollToTop`
  - styles: yes
- **SegmentedTabs** — SegmentedTabs — an iOS-style segmented control with a sliding selected segment (measured transform) plus a subtle press-scale and a soft brand glow on the thumb. Route/nav-flavored: each segment takes an icon + label. Keyboard: arrow keys move between segments, Home/End jump to the ends.
  - exports: `SegmentedTabsItem`, `SegmentedTabsProps`, `SegmentedTabs`
  - styles: yes
- **Sidebar**
  - exports: `SidebarProps`, `SidebarItemProps`, `SidebarItem`, `SidebarGroupProps`, `SidebarGroup`
  - styles: yes
- **Steps**
  - exports: `StepStatus`, `StepsOrientation`, `StepItem`, `StepsProps`, `Steps`
  - styles: yes
- **TabBar** — TabBar — a lightweight segmented top tab bar with an animated underline indicator that slides to the active tab. Unlike Tabs it renders no panels; it only manages a `value`. Keyboard: arrow keys move between tabs.
  - exports: `TabBarItem`, `TabBarProps`, `TabBar`
  - styles: yes
- **Tabs**
  - exports: `TabsVariant`, `TabsSize`, `TabsOrientation`, `TabsProps`, `Tabs`, `TabListProps`, `TabList`, `TabProps`, `Tab`, `TabPanelsProps`, `TabPanels`, `TabPanelProps`, `TabPanel`
  - styles: yes
- **TreeNav** — TreeNav — a collapsible, nested sidebar navigation menu. Groups (items with children) expand/collapse with `aria-expanded`; leaf items highlight when active. Expansion is controlled or uncontrolled.
  - exports: `TreeNavItem`, `TreeNavProps`, `TreeNav`
  - styles: yes
- **TubelightNav** — TubelightNav — a pill navigation with a glowing "tubelight" bar that slides to the active item, casting a soft light bloom (a radial glow plus a bright bar). Keyboard: arrow keys move between items, Home/End jump to the ends.
  - exports: `TubelightNavItem`, `TubelightNavProps`, `TubelightNav`
  - styles: yes
- **Wizard** — Access the surrounding wizard's state and navigation helpers.
  - exports: `WizardStepMeta`, `WizardContextValue`, `useWizard`, `WizardProps`, `WizardStepProps`, `WizardStep`, `Wizard`
  - styles: yes

## overlay

_16 components._

- **AlertDialog**
  - exports: `AlertDialogTone`, `AlertDialogProps`, `AlertDialog`
  - styles: yes
- **Backdrop**
  - exports: `BackdropProps`, `Backdrop`
  - styles: yes
- **ConfirmDialog**
  - exports: `ConfirmDialogTone`, `ConfirmDialogProps`, `ConfirmDialog`
  - styles: yes
- **ContextMenu** — A single actionable row or a separator.
  - exports: `ContextMenuItem`, `ContextMenuProps`, `ContextMenu`
  - styles: yes
- **Drawer**
  - exports: `DrawerPlacement`, `DrawerSize`, `DrawerProps`, `Drawer`
  - styles: yes
- **FloatingActionButton**
  - exports: `FabPlacement`, `FabSize`, `FabTone`, `FloatingActionButtonProps`, `FloatingActionButton`
  - styles: yes
- **HoverCard**
  - exports: `HoverCardPlacement`, `HoverCardProps`, `HoverCard`
  - styles: yes
- **Lightbox**
  - exports: `LightboxImage`, `LightboxProps`, `Lightbox`
  - styles: yes
- **Modal** — Returns true when running in a browser with a DOM available.
  - exports: `ModalSize`, `ModalProps`, `Modal`, `ModalSectionProps`, `ModalHeader`, `ModalBody`, `ModalFooter`
  - styles: yes
- **Popover**
  - exports: `PopoverPlacement`, `PopoverProps`, `Popover`
  - styles: yes
- **Sheet**
  - exports: `SheetProps`, `Sheet`
  - styles: yes
- **Snackbar**
  - exports: `SnackbarPlacement`, `SnackbarProps`, `Snackbar`
  - styles: yes
- **SpeedDial**
  - exports: `SpeedDialDirection`, `SpeedDialPlacement`, `SpeedDialAction`, `SpeedDialProps`, `SpeedDial`
  - styles: yes
- **Spotlight** — Spotlight — dims the page with a highlighted cutout around a target element. The reusable primitive that Tour builds on. Tracks the target across scroll/resize, portals to the body, and is SSR-safe.
  - exports: `SpotlightTarget`, `SpotlightRect`, `SpotlightProps`, `resolveSpotlightTarget`, `Spotlight`
  - styles: yes
- **Toast**
  - exports: `ToastTone`, `ToastPlacement`, `ToastOptions`, `ToastRecord`, `ToastContextValue`, `ToastProviderProps`, `ToastProvider`, `useToast`
  - styles: yes
- **Tour** — Tour — a guided onboarding flow. Highlights each step's target with a Spotlight cutout and anchors a tooltip card with back/next/skip controls and a step counter. Keyboard: Esc skips, ArrowRight/Left navigate. Portals to the body, SSR-safe, with full listener cleanup.
  - exports: `TourPlacement`, `TourStep`, `TourProps`, `Tour`
  - styles: yes

## patterns

_12 components._

- **AnimatedBlob** — A blob whose SVG path morphs continuously between a few organic shapes via SMIL (`<animate>`). Brand-tinted by default — great behind hero content. SSR-safe (static markup; SMIL runs in the browser). The morph pauses under reduced-motion (CSS). Decorative — aria-hidden.
  - exports: `AnimatedBlobProps`, `AnimatedBlob`
  - styles: yes
- **BlobShape** — An organic blob SVG — usable as a decorative backdrop shape. Pick a preset path or pass a `seed` for a unique deterministic blob. Fill with any token color, a raw color, or the brand `"gradient"`. SSR-safe (path computed during render), no motion. Decorative — aria-hidden.
  - exports: `BlobPreset`, `BlobShapeProps`, `BlobShape`
  - styles: yes
- **CirclePattern** — Concentric rings — a ring-burst decorative accent for placing behind or beside headings, badges, and cards. Configurable ring count, size, thickness, color, an optional solid center dot, and an outward opacity fade. SSR-safe (rings computed during render), no motion. Decorative — aria-hidden.
  - exports: `CirclePatternProps`, `CirclePattern`
  - styles: yes
- **CrossPattern** — A tiled plus / cross SVG pattern background — a subtle texture for sections and cards. Configurable spacing, arm length, thickness, and color, with an optional radial fade mask toward the edges. SSR-safe, no motion. Background by default — children render on top. Decorative — aria-hidden.
  - exports: `CrossPatternProps`, `CrossPattern`
  - styles: yes
- **CurveDivider** — A smooth single-arc curve divider between two sections. Choose convex (bulging toward the next section) or concave (scooping inward), on the top or bottom edge. SSR-safe, no motion. Decorative — aria-hidden.
  - exports: `CurveDividerProps`, `CurveDivider`
  - styles: yes
- **DotGridShape** — The classic decorative "dots accent" — a cluster of dots used near headings and cards. Configurable rows/cols, a triangle variant, dot size, gap, and color. SSR-safe (layout computed during render), no motion. Decorative — aria-hidden.
  - exports: `DotGridShapeProps`, `DotGridShape`
  - styles: yes
- **GradientBlob** — A soft, blurred radial-gradient blob for ambient color accents — drop it behind a hero or card for a glow. Tintable, sizeable, purely CSS (no SVG). SSR-safe, no motion. Decorative — aria-hidden. Position it with `style`/className (it is `position: absolute`-friendly via a transparent wrapper).
  - exports: `GradientBlobProps`, `GradientBlob`
  - styles: yes
- **MeshGradient** — A multi-point mesh-gradient panel built from layered radial gradients — a colorful card / section backdrop. Pass your own `colors`, or use the brand-tinted default. Optional slow drift. SSR-safe (gradients composed during render), drift pauses under reduced-motion. Background by default — children render on top.
  - exports: `MeshGradientProps`, `MeshGradient`
  - styles: yes
- **SlantDivider** — A diagonal slanted section edge. The filled triangle attaches to the section; `direction` chooses which corner sits highest and `height` sets the steepness. SSR-safe, no motion. Decorative — aria-hidden.
  - exports: `SlantDividerProps`, `SlantDivider`
  - styles: yes
- **Squiggle** — A hand-drawn-style squiggly accent line for highlighting words — drop it under a heading word or use it as a flourish. Wave, underline, and scribble variants, with an optional draw-on animation. SSR-safe (path computed during render). Draw-on animation respects reduced-motion via CSS. Decorative — aria-hidden.
  - exports: `SquiggleProps`, `Squiggle`
  - styles: yes
- **WaveDivider** — An SVG wave section divider for the top or bottom edge of a section. Configurable amplitude, color (defaults to a surface token so it blends), an optional drift animation, and a layered variant for depth. SSR-safe (path computed during render). Drift pauses under reduced-motion. Decorative — aria-hidden.
  - exports: `WaveDividerProps`, `WaveDivider`
  - styles: yes
- **ZigzagDivider** — A zigzag / triangle-saw edge divider. Teeth are generated to tile the full width; `toothWidth` and `toothHeight` control the saw geometry. SSR-safe, no motion. Decorative — aria-hidden.
  - exports: `ZigzagDividerProps`, `ZigzagDivider`
  - styles: yes

## scheduling

_9 components._

- **AgendaView**
  - exports: `AgendaViewProps`, `AgendaView`
  - styles: yes
- **DayView**
  - exports: `DayViewProps`, `DayView`
  - styles: yes
- **DurationSelect** — Format a minute count as a compact human label, e.g. `1h 30m`.
  - exports: `formatDuration`, `DEFAULT_DURATIONS`, `DurationSelectProps`, `DurationSelect`
  - styles: yes
- **MiniCalendar** — A lightweight event marker for the dot row under a day.
  - exports: `MiniCalendarEvent`, `MiniCalendarProps`, `MiniCalendar`
  - styles: yes
- **Scheduler**
  - exports: `SchedulerViewMode`, `SchedulerProps`, `Scheduler`
  - styles: yes
- **TimeSlotPicker** — One bookable slot.
  - exports: `TimeSlot`, `TimeSlotGroup`, `TimeSlotPickerProps`, `TimeSlotPicker`
  - styles: yes
- **TimezonePicker** — One selectable IANA timezone.
  - exports: `TimezoneOption`, `COMMON_TIMEZONES`, `formatOffset`, `TimezonePickerProps`, `TimezonePicker`
  - styles: yes
- **WeekView**
  - exports: `WeekViewProps`, `WeekView`
  - styles: yes
- **YearView** — Marker contributing to a day's density dot.
  - exports: `YearViewEvent`, `YearViewProps`, `YearView`
  - styles: yes

## showcase

_36 components._

- **AnimatedBeamConnect** — Draws an animated glowing beam (a curved SVG path with a travelling gradient) connecting two node elements by ref within a positioned container — the Magic UI animated-beam. Positions are measured against the container and kept in sync via ResizeObserver + window resize. SSR-safe (all measurement in effects). Reduced motion renders a static resting path.
  - exports: `AnimatedBeamConnectProps`, `AnimatedBeamConnect`
  - styles: yes
- **AnimatedList** — Renders its children as a list where each item springs in (slide + fade). On first paint the whole list staggers in; afterwards only newly-added children replay the entrance, so it doubles as a live, streaming feed — set `origin="top"` to have fresh items push in at the top. New items are detected by key against the previous render. The stagger delay is fed to CSS via a custom property. Reduced motion is respected in CSS.
  - exports: `AnimatedListProps`, `AnimatedList`
  - styles: yes
- **BentoGrid** — Asymmetric "bento box" feature grid. Cards opt into column/row spans through {@link BentoCard}; the grid collapses to a single column on small screens. Pure CSS grid — SSR-safe, no measurement.
  - exports: `BentoGridProps`, `BentoGrid`, `BentoCardProps`, `BentoCard`
  - styles: yes
- **CardHoverGrid** — CardHoverGrid — a grid of cards where hovering one slides a soft highlighted background behind it (the Aceternity hover-effect card grid). The highlight is a single positioned element moved to the hovered card via shared state + CSS transitions, so it appears to glide between cards. SSR-safe, no measurement; respects reduced-motion (the highlight cross-fades instead of sliding via the global duration tokens).
  - exports: `CardHoverItem`, `CardHoverGridProps`, `CardHoverGrid`
  - styles: yes
- **CardsCarousel** — CardsCarousel — a horizontally scroll-snapping rail of large cards that expand into a full overlay on click (the Apple "cards carousel"). Drag-to-scroll with the pointer, arrow buttons, and keyboard (Enter/Space to open, Escape to close). All pointer / scroll / key listeners live in effects with cleanup and guard `window`/`document`, so it is SSR-safe. The scroll snap + transitions slow to near-instant under reduced motion via the global duration tokens.
  - exports: `CarouselCard`, `CardsCarouselProps`, `CardsCarousel`
  - styles: yes
- **CompareReveal** — CompareReveal — a content-agnostic before/after card. A glowing vertical divider wipes between the two states. In `hover` mode the divider tracks the pointer; in `drag` mode it is a keyboard-operable slider (arrow keys), and `autoplay` gently sweeps it when untouched. SSR-safe (autoplay rAF lives in an effect, cleaned up) and the autoplay sweep is disabled under reduced-motion.
  - exports: `CompareRevealProps`, `CompareReveal`
  - styles: yes
- **ContainerScroll** — A 3D "device frame" that starts tilted back and rotates flat (and scales up slightly) as it scrolls into view — the Aceternity container-scroll effect. Drop a screenshot or any media node inside. Scroll progress is read from the wrapper's position and applied via CSS custom properties, updated once per animation frame and cleaned up on unmount. SSR-safe; under reduced motion the frame renders flat.
  - exports: `ContainerScrollProps`, `ContainerScroll`
  - styles: yes
- **ExpandableFeatureCards** — ExpandableFeatureCards — a horizontal row of image panels where the active panel grows (flex-grow) to reveal its content while the others shrink to a slim rail (the "accordion images" pattern). Activates on hover and on focus, fully keyboard accessible: each panel is a button in a tablist, arrow keys and Tab move between them, and the expanded panel is announced via `aria-expanded`. Stateful only — no scroll/observer — so it is SSR-safe. Width transitions slow to near-instant under reduced motion via the global duration tokens.
  - exports: `ExpandableFeatureItem`, `ExpandableFeatureCardsProps`, `ExpandableFeatureCards`
  - styles: yes
- **FeatureSpotlightTabs** — Tabbed feature showcase: a vertical list of features on one side, the active feature's media on the other. The active feature auto-advances on a timer (with a per-item progress bar); hovering the list pauses advancement, and selecting an item resets the timer. Built with `role="tablist"`/`role="tab"` + `tabpanel`. Arrow/Home/End keys move between features. The timer lives in an effect with cleanup; SSR-safe and reduced-motion aware (auto-advance is disabled and the progress bar hidden).
  - exports: `FeatureSpotlightItem`, `FeatureSpotlightTabsProps`, `FeatureSpotlightTabs`
  - styles: yes
- **FeatureSteps** — A feature showcase: a vertical numbered step list paired with a large media panel that crossfades as steps advance, each step showing a progress bar that fills over the interval. Auto-advances (pausable by controlling `activeIndex`) and is clickable. Reduced motion disables the progress fill + autoplay.
  - exports: `FeatureStep`, `FeatureStepsProps`, `FeatureSteps`
  - styles: yes
- **FollowingPointerCard** — A card that hides the native cursor on hover and renders a custom pointer (arrow + trailing label) that follows the cursor inside the card — the Aceternity following-pointer. Pointer position is written to CSS variables on pointer move; the system cursor returns on leave. Reduced motion keeps the native cursor and skips the follower.
  - exports: `FollowingPointerCardProps`, `FollowingPointerCard`
  - styles: yes
- **GeminiBeam** — GeminiBeam — flowing curved SVG beams whose strokes "draw" themselves (stroke-dashoffset) as the section scrolls through the viewport, in the Google-Gemini-effect style. Scroll progress is read against the container and rAF-throttled; listeners clean up on unmount. SSR-safe (beams start undrawn) and, under reduced-motion, the beams render fully drawn with no scroll coupling.
  - exports: `GeminiBeamProps`, `GeminiBeam`
  - styles: yes
- **GlowMenu** — Horizontal nav / tab bar where the active item is highlighted by a glowing pill that slides between items (Linear/Family-style animated highlight). The pill position/size is measured from the active button and applied via CSS custom properties. Keyboard: Arrow keys move between (enabled) items, Home/End jump to the ends. With `as="tablist"` items get `role="tab"`/`aria-selected`; the pill is decorative. SSR-safe and reduced-motion aware (the pill jumps instead of sliding).
  - exports: `GlowMenuItem`, `GlowMenuProps`, `GlowMenu`
  - styles: yes
- **HeroHighlight** — HeroHighlight — a hero backdrop with a dot/grid pattern that brightens in a soft radius under the cursor (the pattern is masked to a pointer-tracked gradient). Pointer position is written to CSS variables in an event handler (no rAF loop); SSR-safe. The reveal is decorative, so touch / reduced-motion simply shows the dim pattern.
  - exports: `HeroHighlightProps`, `HeroHighlight`, `HighlightProps`, `Highlight`
  - styles: yes
- **HorizontalScrollSection** — HorizontalScrollSection — a tall pinned section whose inner track translates horizontally as the user scrolls vertically past it, revealing each panel in turn (galleries, steps, feature reels). Translate distance = track overflow width; progress is read from the section rect once per animation frame (rAF-throttled scroll + resize, cleaned up on unmount) and the track is measured via ResizeObserver. SSR-safe; under reduced motion the track simply scrolls horizontally on its own (no pin).
  - exports: `HorizontalScrollSectionProps`, `HorizontalScrollSection`
  - styles: yes
- **ImageAccordion** — ImageAccordion — a row of images that expand (flex-grow) on hover or focus to reveal a caption, the classic interactive accordion gallery. Fully keyboard accessible: each panel is a focusable button that expands on focus, and Arrow / Home / End move between panels via roving focus. The expand/collapse is a pure CSS flex transition; SSR-safe with no effects. Under reduced motion the resize is instant (handled in CSS).
  - exports: `ImageAccordionItem`, `ImageAccordionProps`, `ImageAccordion`
  - styles: yes
- **ImageTrail** — ImageTrail — as the cursor moves across the surface, a trail of thumbnails spawns along the path and fades out (the award-site "image trail"). Images are pooled and recycled; spawning is throttled by pointer distance and each tile is removed after its lifetime, all inside a single effect with full cleanup. SSR-safe (guards `window`/`document`, listeners attached in the effect). Under reduced motion the trail is disabled entirely and only the children render.
  - exports: `ImageTrailProps`, `ImageTrail`
  - styles: yes
- **LampHeader** — Aceternity-style "lamp" section header: a conic glow fans down from a point and a thin luminous line, illuminating the heading beneath it. Decorative layers are `aria-hidden`; the heading text remains the accessible content. Pure CSS (conic/radial gradients + blur). SSR-safe; entrance animation is disabled under reduced motion via CSS.
  - exports: `LampHeaderProps`, `LampHeader`
  - styles: yes
- **LayoutGridExpand** — LayoutGridExpand — a grid of tiles; clicking one expands it into a large overlay card centred over a backdrop. Click the backdrop, press the tile again, or hit Escape to collapse. Tiles are buttons (keyboard operable), the overlay traps Escape, and the backdrop has a dialog role. SSR-safe (Escape listener lives in an effect) and uses the global duration tokens so motion is reduced automatically.
  - exports: `LayoutGridItem`, `LayoutGridExpandProps`, `LayoutGridExpand`
  - styles: yes
- **LogoOrbit** — Logos/icons orbiting in concentric rings around a center brand mark — the Magic UI orbiting circles applied to a logo cloud. Each ring rotates as a whole while every item counter-rotates so it stays upright. Pure CSS animation; reduced motion freezes the rings into a static constellation.
  - exports: `OrbitRing`, `LogoOrbitProps`, `LogoOrbit`
  - styles: yes
- **MarqueeRow** — Seamless infinite marquee tuned for "trusted by" logo rows: generous gaps, edge fade masks, pause-on-hover, reversible direction, and an optional 3D skew. Content is duplicated so the loop is seamless; the duplicate copy is hidden from assistive tech. Pure CSS animation (SSR-safe, no measurement). Paused under reduced motion.
  - exports: `MarqueeRowProps`, `MarqueeRow`
  - styles: yes
- **MaskedTextReveal** — MaskedTextReveal — a heading uncovered through a wiping clip-path mask as it scrolls into view. Visibility is detected with an IntersectionObserver (disconnected on unmount and after the first reveal when `once`); the wipe itself is a pure CSS clip-path transition. SSR-safe; under reduced motion the heading is shown fully (handled in CSS).
  - exports: `MaskedTextRevealDirection`, `MaskedTextRevealProps`, `MaskedTextReveal`
  - styles: yes
- **ParallaxScrollColumns** — ParallaxScrollColumns — a multi-column gallery whose columns translate at different speeds as the section scrolls through the viewport, for a parallax effect. The scroll handler is rAF-throttled and reads scroll progress relative to the container; listeners are cleaned up on unmount. SSR-safe (starts un-parallaxed) and disabled entirely under reduced-motion.
  - exports: `ParallaxScrollColumnsProps`, `ParallaxScrollColumns`
  - styles: yes
- **PinnedSectionReveal** — PinnedSectionReveal — a tall section whose inner panel pins to the viewport while scrolling advances a horizontal filmstrip through its steps; once the last step is reached the section unpins and the page continues. A progress bar and step dots track position. Scroll progress is read from the section's bounding box in a rAF-throttled listener created in an effect with cleanup; SSR-safe (guards `window`). Under reduced motion the section unpins and stacks the steps vertically.
  - exports: `PinnedStep`, `PinnedSectionRevealProps`, `PinnedSectionReveal`
  - styles: yes
- **ScrollProgress** — Slim progress bar pinned to the top (or bottom) of the viewport reflecting how far the page — or a given scroll container — has been scrolled. The scroll listener is throttled to one update per animation frame and cleaned up on unmount. SSR-safe (no window access during render). Marked `aria-hidden` as it mirrors visible scroll position.
  - exports: `ScrollProgressProps`, `ScrollProgress`
  - styles: yes
- **ScrollReveal** — Generic on-scroll reveal wrapper. Fades/slides/scales/blurs its child (or, in `stagger` mode, each direct child in sequence) into view when it enters the viewport. Visibility is detected with an IntersectionObserver, disconnected on unmount (and after the first reveal when `once`). SSR-safe; respects reduced motion via CSS (content shows immediately, no transform).
  - exports: `ScrollRevealAnimation`, `ScrollRevealProps`, `ScrollReveal`
  - styles: yes
- **ScrollTimeline** — ScrollTimeline — a vertical timeline whose entries fade/slide in as they enter the viewport, with the connecting spine drawing itself top-to-bottom as you scroll past the section (IntersectionObserver per entry + a rAF scroll read for the line height). SSR-safe: the observer and scroll listener are created in an effect that guards `window`, with full cleanup. Under reduced motion every entry renders revealed and the spine is full-height immediately.
  - exports: `TimelineEntry`, `ScrollTimelineProps`, `ScrollTimeline`
  - styles: yes
- **ScrollVelocitySkew** — ScrollVelocitySkew — a continuously drifting marquee whose text skews and stretches based on scroll velocity: the faster you scroll, the more the row leans and the faster it travels; scroll direction flips the drift direction. Returns to a gentle idle drift when scrolling stops. A single rAF loop integrates position from a decaying velocity derived from `window.scrollY` deltas; the loop and scroll listener are created in an effect with cleanup and guard `window`, so it is SSR-safe. Under reduced motion the row is static with no skew.
  - exports: `ScrollVelocitySkewProps`, `ScrollVelocitySkew`
  - styles: yes
- **SpotlightCardGrid** — SpotlightCardGrid — a grid of cards sharing ONE cursor-following radial spotlight. As the pointer moves over the grid, a single glow tracks it and lights up whichever card sits beneath, with each card's border catching the light. The pointer position is written to CSS variables in a rAF-throttled pointermove handler (cleaned up on unmount). SSR-safe; cards are focusable so keyboard users get the same highlight via :focus-within. The glow is purely decorative and disabled under reduced motion.
  - exports: `SpotlightCardGridItem`, `SpotlightCardGridProps`, `SpotlightCardGrid`
  - styles: yes
- **SpotlightFeatureCard** — SpotlightFeatureCard — a premium feature card with a cursor-following radial spotlight on the border/surface AND a secondary layer that slides up to reveal extra content (CTA, metrics) on hover or keyboard focus. The pointer position is written to CSS custom properties via a rAF-throttled `pointermove` listener attached in an effect with cleanup; SSR-safe (guards `window`). Under reduced motion the spotlight centers statically and the reveal layer shows without sliding.
  - exports: `SpotlightFeatureCardProps`, `SpotlightFeatureCard`
  - styles: yes
- **StackingCards** — StackingCards — cards that pin to the top of the viewport one after another as you scroll; each card sticks, then the next slides up and overlaps it, the earlier card scaling back slightly to create a deck. Built on CSS `position: sticky` plus a rAF scroll read that scales/dims the card behind the incoming one. SSR-safe: the scroll listener is created in an effect that guards `window` and cleans up. Under reduced motion the cards render as a simple stacked column with no scaling.
  - exports: `StackingCard`, `StackingCardsProps`, `StackingCards`
  - styles: yes
- **StickyScrollReveal** — Two-column scroll story: a sticky visual panel on one side swaps its content as the reader scrolls through stacked text sections on the other. The active section is whichever is nearest the vertical middle of the viewport. Active detection uses an IntersectionObserver with a centered root margin; the observer is rebuilt when items change and disconnected on unmount. SSR-safe.
  - exports: `StickyScrollItem`, `StickyScrollRevealProps`, `StickyScrollReveal`
  - styles: yes
- **TestimonialsMarquee** — A polished testimonials wall: quote cards marquee horizontally across multiple rows in alternating directions, with optional pause-on-hover and edge fade. Pure CSS animation (content duplicated for a seamless loop); reduced motion is handled in CSS by halting the animation.
  - exports: `Testimonial`, `TestimonialsMarqueeProps`, `TestimonialsMarquee`
  - styles: yes
- **ThreeDMarquee** — ThreeDMarquee — a perspective-skewed grid of images sliding diagonally across a tilted 3D plane (the Aceternity "3D marquee wall"). The whole grid is rotated in 3D via CSS `transform`; each column drifts vertically on a CSS animation, alternating direction for a parallax weave. Pure CSS motion — no scroll/rAF needed, so it is fully SSR-safe. Under reduced-motion the columns hold still (animation paused via a data attribute).
  - exports: `ThreeDMarqueeProps`, `ThreeDMarquee`
  - styles: yes
- **TracingBeam** — A vertical guide line down the left of long content with a glowing beam + dot that tracks the reader's scroll position through the content — ideal for articles and timelines. Progress is computed from the content's bounding rect relative to the viewport, updated once per animation frame on scroll/resize and cleaned up on unmount. SSR-safe; decorative track is `aria-hidden`.
  - exports: `TracingBeamProps`, `TracingBeam`
  - styles: yes
- **ZoomParallax** — ZoomParallax — a classic scroll-driven zoom gallery. A tall section pins a sticky stage; as you scroll through it the central image scales up to fill the viewport while the surrounding images parallax outward at varying rates. Scroll progress is read from the section's bounding rect once per animation frame (rAF-throttled scroll + resize listeners, cleaned up on unmount) and pushed to CSS custom properties. SSR-safe; under reduced motion the stage renders static via CSS.
  - exports: `ZoomParallaxItem`, `ZoomParallaxProps`, `ZoomParallax`
  - styles: yes

## templates

_8 components._

- **AuthLayout** — AuthLayout — centered authentication shell. A single card floats over a subtle (optionally gradient) background, with brand, title/subtitle, the form (children) and a footer slot. Responsive and SSR-safe. Self-contained: does not import other Verve components.
  - exports: `AuthLayoutProps`, `AuthLayout`
  - styles: yes
- **BlankCenteredLayout** — BlankCenteredLayout — minimal single-column centered shell for simple pages (onboarding steps, confirmations, interstitials). A constrained column with optional brand and footer slots. SSR-safe and self-contained.
  - exports: `BlankCenteredLayoutProps`, `BlankCenteredLayout`
  - styles: yes
- **DashboardLayout** — DashboardLayout — application dashboard shell on CSS grid. A sticky top bar (brand, search, actions), a left navigation sidebar, a main content area with an optional page header, and an optional right rail. On small screens the sidebar becomes off-canvas, toggled via the controlled `sidebarOpen` prop. Each region maps to a semantic landmark. SSR-safe and self-contained.
  - exports: `DashboardLayoutProps`, `DashboardLayout`
  - styles: yes
- **DocsLayout** — DocsLayout — documentation shell on CSS grid. A left navigation sidebar, a centered content column (children) and a right "on this page" TOC rail, under an optional sticky top bar. Both rails hide on small screens, leaving the content full-width. Landmarks for nav/main/aside. SSR-safe, self-contained.
  - exports: `DocsLayoutProps`, `DocsLayout`
  - styles: yes
- **ErrorLayout** — ErrorLayout — full-page error / empty-state screen. Centers a large code or illustration, a title, a description, action buttons and optional support links. SSR-safe and self-contained.
  - exports: `ErrorLayoutProps`, `ErrorLayout`
  - styles: yes
- **MarketingLayout** — MarketingLayout — landing-page wrapper. A sticky nav (brand, links, CTA) that transitions from transparent to a solid surface as the page scrolls (pure CSS via a sentinel + sticky element), the main content, and a footer. SSR-safe and self-contained.
  - exports: `MarketingLayoutProps`, `MarketingLayout`
  - styles: yes
- **SettingsLayout** — SettingsLayout — settings page shell on CSS grid. A vertical section nav on the left and a content panel on the right with a page title. On small screens the nav becomes a horizontal scroller above the panel. Landmarks for nav/main. SSR-safe and self-contained.
  - exports: `SettingsLayoutProps`, `SettingsLayout`
  - styles: yes
- **SplitAuthLayout** — SplitAuthLayout — two-column authentication shell. One column hosts the brand + form, the other a decorative marketing panel. Collapses to a single column on small screens (the aside drops below the form, or hides if empty). Reversible via `reverse`. SSR-safe and self-contained.
  - exports: `SplitAuthLayoutProps`, `SplitAuthLayout`
  - styles: yes

## texteffects

_37 components._

- **AuroraText** — AuroraText — fills the text with a slowly shifting multi-color aurora gradient (animated `background-clip: text` + drifting position/hue). CSS-only, SSR-safe, and freezes to a static fill under reduced-motion.
  - exports: `AuroraTextProps`, `AuroraText`
  - styles: yes
- **BlurFadeText** — BlurFadeText — each word blurs in and fades up with a stagger when scrolled into view (or on mount). Uses IntersectionObserver inside an effect (SSR-safe) and snaps to crisp, fully-visible text under reduced-motion.
  - exports: `BlurFadeTextProps`, `BlurFadeText`
  - styles: yes
- **BoxReveal** — BoxReveal — a colored box wipes across the content then retracts, uncovering the text underneath (the classic Magic-UI reveal). Triggered on scroll-into- view via IntersectionObserver in an effect (SSR-safe). Under reduced-motion the content is shown immediately with no box sweep.
  - exports: `BoxRevealDirection`, `BoxRevealProps`, `BoxReveal`
  - styles: yes
- **CircularText** — CircularText — lays each character around a circle (rotated per-glyph) inside an optionally, slowly spinning ring. Pure CSS layout + animation, no JS loop, SSR-safe. Under reduced-motion the ring stops spinning but the circular layout is preserved.
  - exports: `CircularTextProps`, `CircularText`
  - styles: yes
- **ColourfulText** — ColourfulText — every letter has a shifting hue, continuously animating through the rainbow with a per-letter offset so the colours flow across the word. Pure CSS animation (no JS loop); SSR-safe. Under reduced-motion the hue cycle freezes into a static gradient spread.
  - exports: `ColourfulTextProps`, `ColourfulText`
  - styles: yes
- **CountingNumber** — CountingNumber — an odometer-style number that rolls each digit up to its value: every digit column physically slides through 0–9 to settle on its target, easing as it lands. Triggered on scroll-into-view (IntersectionObserver in an effect, SSR-safe). Under reduced-motion it snaps straight to the formatted final value with no rolling.
  - exports: `CountingNumberProps`, `CountingNumber`
  - styles: yes
- **DecryptedText** — DecryptedText — every character flickers through a matrix-rain symbol set while a reveal sweep settles the real letters left-to-right (a "decrypt" effect, distinct from a per-letter hover scramble). Driven by a single frame timer in an effect with cleanup; SSR-safe. Under reduced-motion the final text is shown immediately with no scrambling.
  - exports: `DecryptedTextProps`, `DecryptedText`
  - styles: yes
- **FallingText** — FallingText — letters drop in from above with a gravity-and-bounce settle, each offset by a stagger so the word assembles in sequence. Triggers on mount or when the element scrolls into view (IntersectionObserver, disconnected on cleanup); SSR-safe. Under reduced-motion the text appears in place with no drop.
  - exports: `FallingTextProps`, `FallingText`
  - styles: yes
- **FlipText** — FlipText — each character flips into place around the 3D X-axis with a stagger, like falling tiles. Triggered on mount or scroll-into-view (IntersectionObserver in an effect, SSR-safe). Under reduced-motion the text is shown flat with no flip.
  - exports: `FlipTextProps`, `FlipText`
  - styles: yes
- **FlipWords** — FlipWords — cycles through a list of words with a 3D spring flip, e.g. "make it ___" → beautiful / modern / fast. Driven by a single timer in an effect (cleaned up on unmount); SSR-safe. Under reduced-motion the words still cycle but cross-fade instead of flipping.
  - exports: `FlipWordsProps`, `FlipWords`
  - styles: yes
- **GlitchText** — GlitchText — a datamosh / RGB-split glitch. Two clipped clone layers (cyan + magenta) jitter and clip-slice over the base text. Pure CSS animation; SSR-safe. With `hoverOnly` the glitch is idle until hover. Respects reduced-motion (layers hide, base text stays crisp).
  - exports: `GlitchTextProps`, `GlitchText`
  - styles: yes
- **GradientUnderline** — GradientUnderline — wraps a heading/phrase with an animated gradient underline that draws in from the left (on view or hover) and then keeps shimmering. The draw is triggered via IntersectionObserver (view) or pointer (hover); observer disconnected on cleanup, SSR-safe. Under reduced-motion the underline is shown full-width with no draw or shimmer.
  - exports: `GradientUnderlineProps`, `GradientUnderline`
  - styles: yes
- **HighlightSweep** — HighlightSweep — wraps key words so a brand highlight bar wipes in behind them (left to right) the moment they scroll into view, like a marker stroke. Visibility is detected with an IntersectionObserver (disconnected on unmount and after the first sweep when `once`). The sweep is a pure CSS clip-path transition. SSR-safe; reduced motion shows the highlight already drawn.
  - exports: `HighlightSweepProps`, `HighlightSweep`
  - styles: yes
- **HyperText** — HyperText — characters cycle through random glyphs, then lock into the final text left-to-right ("decode" effect), on mount and/or hover. Driven by a single timer in an effect with cleanup; SSR-safe and, under reduced-motion, shows the final text immediately with no scrambling.
  - exports: `HyperTextProps`, `HyperText`
  - styles: yes
- **LineShadowText** — LineShadowText — a bold display heading backed by a striped diagonal "line shadow" offset behind the letters, the stripes drifting continuously (the trendy line-shadow display style). Pure CSS via a duplicated text layer, so it is SSR-safe; under reduced-motion the stripes hold still.
  - exports: `LineShadowTextProps`, `LineShadowText`
  - styles: yes
- **MorphingText** — MorphingText — smoothly cross-fades between strings, the outgoing word blurring and fading out while the incoming one sharpens in, looping forever. Two stacked layers are swapped by a timer in an effect (with cleanup); SSR- safe and, under reduced-motion, just shows the first string with no looping.
  - exports: `MorphingTextProps`, `MorphingText`
  - styles: yes
- **NeonText** — NeonText — glowing neon-sign text built from stacked `text-shadow` halos. Optional flicker animates a few stops to mimic a buzzing tube. Pure CSS; SSR-safe. Under reduced-motion the flicker is disabled and the sign stays lit steadily.
  - exports: `NeonTextProps`, `NeonText`
  - styles: yes
- **OutlineText** — OutlineText — hollow display type drawn with a text-stroke only. The fill can sweep in on hover or when the element scrolls into view (left-to-right clip-path wipe). Pure CSS animation; the view trigger uses an IntersectionObserver with cleanup. SSR-safe; reduced motion snaps the fill.
  - exports: `OutlineTextFill`, `OutlineTextProps`, `OutlineText`
  - styles: yes
- **PointerHighlight** — PointerHighlight — wraps a phrase and, on view or hover, draws a brand highlighter box around it with small corner brackets (the Aceternity pointer-highlight). View triggering uses IntersectionObserver (disconnected on cleanup); SSR-safe. Under reduced-motion the box is shown immediately with no draw animation.
  - exports: `PointerHighlightProps`, `PointerHighlight`
  - styles: yes
- **RotatingText** — RotatingText — cycles a list of words, with each character of the outgoing word sliding/fading out and each character of the incoming word sliding/fading in on a per-letter stagger (distinct from a plain word-swap). A single timer advances the index; cleanup on unmount, SSR-safe. Under reduced-motion the words swap instantly with no per-letter choreography.
  - exports: `RotatingTextProps`, `RotatingText`
  - styles: yes
- **ScrambleHover** — ScrambleHover — text scrambles through random glyphs then resolves left to right when hovered or focused ("decode on hover"). Distinct from HyperText, which decodes on mount. A single timer drives the scramble and is cleared on unmount; SSR-safe. Under reduced-motion it never scrambles — the text is always shown resolved.
  - exports: `ScrambleHoverProps`, `ScrambleHover`
  - styles: yes
- **ScrambleIn** — ScrambleIn — characters spawn from an empty line, each flickering through a glyph pool for a moment before locking onto its final letter (a per-character reveal-from-blank, distinct from DecryptedText's left-to-right decrypt sweep). Triggers when scrolled into view via IntersectionObserver. Driven by a single frame timer; SSR-safe with cleanup. Reduced motion shows the text immediately.
  - exports: `ScrambleInProps`, `ScrambleIn`
  - styles: yes
- **ScrollTextReveal** — ScrollTextReveal — a block of text whose words brighten from dim to full one after another as the element scrolls through the viewport (the Magic UI scroll "text reveal" effect). Uses a scroll listener mapped to the element's progress through the viewport; rAF-throttled with cleanup, SSR-safe. Under reduced-motion every word is shown at full opacity immediately.
  - exports: `ScrollTextRevealProps`, `ScrollTextReveal`
  - styles: yes
- **ShimmerHeading** — ShimmerHeading — a large headline with a subtle silver shimmer that sweeps across the text. Tuned for headline sizing (heavy weight, tight leading), distinct from ShinyText's small label treatment. Pure CSS background-clip animation; SSR-safe and renders semantic heading levels. The sweep stops under reduced-motion, leaving a clean static heading.
  - exports: `ShimmerHeadingProps`, `ShimmerHeading`
  - styles: yes
- **ShinyGradientHeading** — ShinyGradientHeading — a large hero heading painted with a moving multi-stop gradient and an optional soft glow. The gradient is clipped to the text and animated via background-position; pure CSS, SSR-safe. Under reduced motion the sweep halts on a static gradient.
  - exports: `ShinyGradientHeadingProps`, `ShinyGradientHeading`
  - styles: yes
- **ShinyText** — ShinyText — a metallic light highlight sweeps continuously across the text via an animated masked gradient. Perfect for "Pro" / premium labels. CSS-only, so it is SSR-safe and honors reduced-motion (the sweep simply holds still).
  - exports: `ShinyTextProps`, `ShinyText`
  - styles: yes
- **SparklesText** — SparklesText — little twinkling stars pop at random positions over and around the text, each fading + scaling on its own loop. Positions are seeded so the server and first client render agree (SSR-safe), then re-randomize on an interval purely on the client. Under reduced-motion the sparkles are hidden.
  - exports: `SparklesTextProps`, `SparklesText`
  - styles: yes
- **SplitFlapText** — SplitFlapText — an airport/train solari split-flap display. Each column flips through the charset until it lands on its target glyph, columns settling left to right. Driven by a single interval timer in an effect with cleanup; SSR-safe. Under reduced motion the final text is shown immediately.
  - exports: `SplitFlapTextProps`, `SplitFlapText`
  - styles: yes
- **StaggerReveal** — StaggerReveal — splits text into words or lines and reveals each token in sequence (slide / fade / blur) once the block scrolls into view. Visibility is detected with an IntersectionObserver that disconnects on unmount (and after the first reveal when `once`). Stagger is expressed as CSS variables so the animation itself is GPU-driven. SSR-safe; reduced motion shows everything immediately (handled in CSS).
  - exports: `StaggerRevealSplit`, `StaggerRevealEffect`, `StaggerRevealProps`, `StaggerReveal`
  - styles: yes
- **TextGenerateEffect** — TextGenerateEffect — words fade (and optionally blur) in one-by-one, like an AI streaming a response. Runs on mount, or when scrolled into view via IntersectionObserver. SSR-safe (observer in an effect, cleaned up) and snaps fully visible under reduced-motion.
  - exports: `TextGenerateEffectProps`, `TextGenerateEffect`
  - styles: yes
- **TextHoverFill** — TextHoverFill — SVG outline text whose gradient fill is revealed through a soft circular mask that follows the cursor across the letters (Aceternity "text hover" effect). Pointer math runs in event handlers only (no rAF loop); SSR-safe. When idle / on touch devices it shows the outline; the fill simply never reveals without a pointer, which is an acceptable graceful fallback.
  - exports: `TextHoverFillProps`, `TextHoverFill`
  - styles: yes
- **TextReveal** — TextReveal — words or characters fade in and rise into place with a stagger, either on mount or when scrolled into view via IntersectionObserver. SSR-safe (observer lives in an effect) and snaps fully visible under reduced-motion.
  - exports: `TextRevealProps`, `TextReveal`
  - styles: yes
- **TrueFocus** — TrueFocus — steps through a sentence focusing one word at a time: the active word snaps sharp inside a brand outline box while the rest stay blurred, advancing on a timer (the reactbits TrueFocus). A single interval drives the index; cleanup on unmount, SSR-safe. Under reduced-motion no blur is applied and the words do not cycle.
  - exports: `TrueFocusProps`, `TrueFocus`
  - styles: yes
- **TypingLoop** — TypingLoop — types and deletes through a list of phrases on a loop with a blinking caret. Distinct from a single-string Typewriter: it cycles multiple phrases with independent type / delete speeds and a configurable start delay. Scheduling uses one chained setTimeout in an effect with cleanup; SSR-safe. Under reduced motion the first phrase is shown in full with no animation.
  - exports: `TypingLoopProps`, `TypingLoop`
  - styles: yes
- **VariableProximity** — VariableProximity — each character's weight and scale ramp up the closer the cursor gets, falling back to the resting weight beyond the radius (a variable-font proximity effect). Pointer position is read on pointermove and applied per-frame via requestAnimationFrame; listeners and the frame are cleaned up on unmount, and the whole thing is SSR-safe. Under reduced-motion the text stays at its resting weight with no pointer tracking.
  - exports: `VariableProximityProps`, `VariableProximity`
  - styles: yes
- **WavyText** — WavyText — each letter bobs up and down in a continuous sine wave, offset by a per-letter delay so the motion travels across the word. Pure CSS animation, no JS loop, SSR-safe. Under reduced-motion the letters stay flat with no bob.
  - exports: `WavyTextProps`, `WavyText`
  - styles: yes
- **WordRotate** — WordRotate — cycles through a list of words in place, each sliding or flipping out as the next enters (great for "Build ___ faster"). Driven by a single timer in an effect with cleanup; SSR-safe and, under reduced-motion, swaps words instantly with no transform.
  - exports: `WordRotateTransition`, `WordRotateProps`, `WordRotate`
  - styles: yes

## threed

_9 components._

- **CardCarousel3D** — A rotating 3D ring of cards. Items sit evenly around a cylinder; the ring rotates to bring the selected card to the front. Auto-advance freezes under reduced-motion; keyboard + drag move selection.
  - exports: `CardCarousel3DItem`, `CardCarousel3DProps`, `CardCarousel3D`
  - styles: yes
- **Coverflow** — iTunes-style coverflow carousel. The centered cover sits flat and large while neighbours rotate away into perspective. Drag, arrows, and keyboard arrows move selection.
  - exports: `CoverflowItem`, `CoverflowProps`, `Coverflow`
  - styles: yes
- **FlipBox** — A box that rotates to reveal a different face. With two faces it is a classic front/back flip; with more it cycles like a reel. Trigger by hover, click, or auto (auto freezes under reduced-motion).
  - exports: `FlipBoxTrigger`, `FlipBoxAxis`, `FlipBoxProps`, `FlipBox`
  - styles: yes
- **FloatingCube** — A slowly auto-rotating 3D cube with six content faces. Freezes under `prefers-reduced-motion` while keeping every face readable.
  - exports: `FloatingCubeFace`, `FloatingCubeProps`, `FloatingCube`
  - styles: yes
- **FoldCard** — A card that unfolds in 3D: a cover panel rotates open from a hinge to reveal content behind it. Opens on hover or click.
  - exports: `FoldCardTrigger`, `FoldCardProps`, `FoldCard`
  - styles: yes
- **IsometricCard** — A card rendered in an isometric / tilted 3D projection with extruded side walls for depth. Lifts toward the viewer on hover.
  - exports: `IsometricCardProps`, `IsometricCard`
  - styles: yes
- **ParallaxLayers** — A multi-layer 3D depth card. Layers sit at different `translateZ` depths and shift parallax-style as the card tilts toward the cursor. Tilt + parallax freeze under reduced-motion while layers stay legible.
  - exports: `ParallaxLayer`, `ParallaxLayersProps`, `ParallaxLayers`
  - styles: yes
- **PerspectiveCardStack** — A stack of cards receding into depth. The top card peels forward on hover and can be sent to the back on click, advancing the stack.
  - exports: `PerspectiveCardStackItem`, `PerspectiveCardStackProps`, `PerspectiveCardStack`
  - styles: yes
- **RotatingPrism** — A prism (triangular, hexagonal, …) that rotates to present each face like a 3D billboard. Auto-advances on an interval that freezes under reduced-motion.
  - exports: `RotatingPrismProps`, `RotatingPrism`
  - styles: yes

## typography

_20 components._

- **Abbr** — Accessible `<abbr>` with a dotted underline. The `definition` is surfaced via the native `title` attribute (hover tooltip + AT) and the visual underline hints that the term is explainable.
  - exports: `AbbrProps`, `Abbr`
  - styles: yes
- **AnchorHeading** — A heading with a hover-revealed `#` anchor link that targets its own id and copies the resulting URL hash to the clipboard on click. Built for docs.
  - exports: `AnchorHeadingLevel`, `AnchorHeadingProps`, `AnchorHeading`
  - styles: yes
- **Blockquote**
  - exports: `BlockquoteProps`, `Blockquote`
  - styles: yes
- **Caption** — Caption — small caption / label text for figures, form fields and metadata. Optional uppercase + tracking treatment for eyebrow-style labels.
  - exports: `CaptionElement`, `CaptionTone`, `CaptionProps`, `Caption`
  - styles: yes
- **Code**
  - exports: `CodeProps`, `Code`
  - styles: yes
- **Emphasis**
  - exports: `EmProps`, `Em`, `StrongProps`, `Strong`
  - styles: yes
- **Glossary** — A styled definition list for glossaries: each entry renders a bold term (`<dt>`) above its definition (`<dd>`). Distinct from data-display's DescriptionList, which is for inline key/value metadata.
  - exports: `GlossaryEntry`, `GlossaryProps`, `Glossary`
  - styles: yes
- **GradientText** — GradientText — clips a brand gradient to the text fill. Use a `preset` or supply custom `from`/`to` stops. Degrades to `fallbackColor` where `background-clip: text` is unsupported.
  - exports: `GradientPreset`, `GradientTextProps`, `GradientText`
  - styles: yes
- **Heading** — Sensible visual size per semantic level when `size` is not supplied.
  - exports: `HeadingLevel`, `HeadingSize`, `HeadingWeight`, `HeadingProps`, `Heading`
  - styles: yes
- **Highlighter** — Highlights every occurrence of `query` within `text` by wrapping matches in `<mark>` elements. Case-insensitive by default; supports multiple terms.
  - exports: `HighlighterProps`, `Highlighter`
  - styles: yes
- **Kbd**
  - exports: `KbdSize`, `KbdProps`, `Kbd`
  - styles: yes
- **Lead** — Lead — large intro paragraph that sits below a heading. Muted-strong tone, relaxed leading, designed to set up the body copy that follows.
  - exports: `LeadSize`, `LeadProps`, `Lead`
  - styles: yes
- **Link**
  - exports: `LinkVariant`, `LinkProps`, `Link`
  - styles: yes
- **Mark** — Mark / Highlight — highlighted inline text with a token-based background tint per tone. Renders a semantic `<mark>` element.
  - exports: `MarkTone`, `MarkProps`, `Mark`
  - styles: yes
- **NumberFormat** — Formats a number as text via `Intl.NumberFormat` (currency / percent / decimal / compact) with size and tone styling. Pure and static — no animation, unlike `AnimatedCounter`.
  - exports: `NumberFormatStyle`, `NumberFormatSize`, `NumberFormatTone`, `NumberFormatProps`, `NumberFormat`
  - styles: yes
- **Prose** — Prose — rich-text / markdown container. Styles raw HTML children (headings, paragraphs, lists, blockquotes, code, tables, images, …) with good vertical rhythm using only semantic + typographic tokens.
  - exports: `ProseSize`, `ProseProps`, `Prose`
  - styles: yes
- **PullQuote** — An oversized decorative pull-quote for editorial layouts: large accent text with an optional float (left/right) so body copy wraps around it. For emphasis of a line already present in the article — distinct from `Blockquote`.
  - exports: `PullQuoteFloat`, `PullQuoteProps`, `PullQuote`
  - styles: yes
- **Text**
  - exports: `TextElement`, `TextSize`, `TextWeight`, `TextTone`, `TextAlign`, `TextProps`, `Text`
  - styles: yes
- **TextList** — TextList — styled ordered / unordered list for typography contexts. Named distinctly from the data-display `List` to avoid collisions.
  - exports: `TextListMarker`, `TextListSpacing`, `TextListProps`, `TextList`, `TextListItemProps`, `TextListItem`
  - styles: yes
- **Truncate** — Truncate — single- or multi-line truncation wrapper. Set `lines` for the clamp count and `title` to expose the full text on hover.
  - exports: `TruncateProps`, `Truncate`
  - styles: yes

## utility

_66 components._

- **ClickAwayListener** — Calls `onClickAway` when a `pointerdown` happens outside its single child. SSR-safe (listeners are attached in an effect). The child must accept a ref.
  - exports: `ClickAwayListenerProps`, `ClickAwayListener`
  - styles: no
- **ClientOnly** — Renders `children` only after the component has mounted in the browser, rendering `fallback` (default `null`) on the server and the first client pass. Use to wrap browser-only UI and avoid hydration mismatches.
  - exports: `ClientOnlyProps`, `ClientOnly`
  - styles: no
- **ColorModeScript** — Inline `<script>` that sets the theme attribute on the document root **before first paint**, eliminating the flash of incorrect theme (FOUC) on initial load and hydration. Render it in the document `<head>` (e.g. Next.js `_document`, or any SSR head) as early as possible. It reads the same `storageKey` the `ThemeProvider` writes (default `"nova-theme"`). This is pure markup — SSR-friendly and emits no client runtime beyond the one-shot inline script.
  - exports: `ColorModeScriptProps`, `ColorModeScript`
  - styles: no
- **FocusTrap** — Traps Tab / Shift+Tab focus within its children while `active`. On activation it (optionally) focuses the first focusable element and remembers what was focused before; on deactivation it (optionally) restores that focus. SSR-safe — all DOM access happens inside effects.
  - exports: `FocusTrapProps`, `FocusTrap`
  - styles: no
- **LiveRegion** — An `aria-live` region for announcing dynamic messages to assistive technology. Render it once (typically near the app root) and update its `children` to make announcements; or drive it with {@link useAnnouncer}. Visually hidden by default via the shared `.nova-visually-hidden` utility.
  - exports: `LiveRegionPoliteness`, `LiveRegionProps`, `LiveRegion`
  - styles: no
- **NoSsr** — Like {@link ClientOnly}, but defers rendering by an extra animation frame after mount. Handy when children must wait for layout to settle (e.g. measuring the DOM) or to push expensive client-only work off the critical hydration path. SSR-safe: renders `fallback` until the deferred frame runs in the browser.
  - exports: `NoSsrProps`, `NoSsr`
  - styles: no
- **Portal** — Render children into a different part of the DOM via `createPortal`. SSR-safe: renders nothing on the server and until the first client effect runs, then portals into `container` (default `document.body`).
  - exports: `PortalProps`, `Portal`
  - styles: no
- **Show** — Conditionally render children by viewport breakpoint and/or a boolean. - `<Show above="md">` — visible at ≥ 768px. - `<Show below="sm">` — visible at < 640px. - `<Show above="md" below="xl">` — visible in the [md, xl) range. - `<Show when={isOpen}>` — plain boolean gate (no breakpoint needed). SSR-safe via the `ssr` prop, which seeds the match value before hydration. With no `above`/`below`/`when` props it always renders its children.
  - exports: `NOVA_BREAKPOINTS`, `Breakpoint`, `ShowProps`, `Show`
  - styles: no
- **SkipLink** — "Skip to content" link — visually hidden until focused, then revealed at the top-left of the viewport. Place as the first focusable element in the DOM so keyboard / screen-reader users can bypass repeated navigation. The `href` is the bare target id (e.g. `"main"` → `href="#main"`). Children default to `"Skip to content"`.
  - exports: `SkipLinkProps`, `SkipLink`
  - styles: yes
- **ThemeProvider** — Provides theme context and renders a `div.nova-root` carrying `data-theme`. - Controlled via `theme` or uncontrolled via `defaultTheme`. - Persists the preference to `localStorage` (SSR-safe; key configurable). - When the preference is `"system"`, follows `prefers-color-scheme` live. All `window` / `localStorage` / `matchMedia` access is guarded behind effects so the component is safe to render on the server.
  - exports: `Theme`, `ResolvedTheme`, `ThemeContextValue`, `ThemeProviderProps`, `ThemeProvider`, `useTheme`
  - styles: no
- **ThemeToggle** — Polished light/dark toggle backed by {@link useTheme}. Renders a single accessible button with an animated sun ↔ moon crossfade/morph. - `aria-pressed` reflects dark mode being active. - Provides a dynamic `aria-label` (override with `label`). - Must be rendered inside a `<ThemeProvider>`.
  - exports: `ThemeToggleProps`, `ThemeToggle`
  - styles: yes
- **useAnnouncer** — Imperatively announce messages to screen readers without rendering a component. Lazily injects a pair of visually-hidden `aria-live` regions (one polite, one assertive) into `document.body` on first use and cleans them up on unmount. SSR-safe: all DOM access is deferred to event handlers / effects, so calling the hook on the server does nothing until `announce` runs in the browser. const { announce } = useAnnouncer(); announce("Saved"); announce("Upload failed", "assertive");
  - exports: `UseAnnouncerOptions`, `UseAnnouncerReturn`, `useAnnouncer`
  - styles: no
- **useClipboard** — Copy text to the clipboard with transient `copied` feedback. SSR-safe: guards `navigator.clipboard` (and falls back to a hidden `textarea` + `execCommand` where the async API is unavailable). The `copied` flag auto-resets after `timeout` ms; the reset timer is cleared on unmount.
  - exports: `UseClipboardOptions`, `UseClipboardReturn`, `useClipboard`
  - styles: no
- **useControllableState** — Generic controlled/uncontrolled state hook. - **Controlled:** pass `value` (and usually `onChange`). The hook mirrors the incoming `value` and never owns internal state; the updater just calls `onChange` with the resolved next value. - **Uncontrolled:** omit `value` and (optionally) pass `defaultValue`. The hook owns the state and still calls `onChange` on every update. The control mode is locked on first render (a component should not flip between controlled and uncontrolled), matching React's own conventions.
  - exports: `UseControllableStateOptions`, `UseControllableStateReturn`, `useControllableState`
  - styles: no
- **useCookie** — Reads and writes a single cookie via `document.cookie`. SSR-safe: returns `null` on the server (guards `document`). Cookie names and values are URL-encoded. Returns `[value, setCookie, removeCookie]`. `removeCookie` expires the cookie immediately (pass matching `path`/`domain` so the browser targets the right cookie).
  - exports: `CookieOptions`, `UseCookieReturn`, `useCookie`
  - styles: no
- **useCounter** — Numeric counter with optional min/max clamping. Returns `{ count, increment, decrement, set, reset }`. The action callbacks are stable across renders.
  - exports: `UseCounterOptions`, `UseCounterReturn`, `useCounter`
  - styles: no
- **useDebouncedCallback** — Returns a debounced version of `callback` that delays invocation until `delay` ms have elapsed since the last call. The latest `callback` is always used (kept in a ref) so the debounced function identity is stable. Any pending call is cancelled on unmount.
  - exports: `DebouncedFunction`, `useDebouncedCallback`
  - styles: no
- **useDebouncedValue** — Returns a debounced copy of `value` that only updates `delay` ms after the input stops changing. The pending timer is cleared whenever `value` (or `delay`) changes and on unmount, so rapid updates collapse into a single trailing change.
  - exports: `useDebouncedValue`
  - styles: no
- **useDisclosure** — Manage boolean open/closed state for disclosures (modals, drawers, menus…). Supports both controlled (`open` + `onChange`) and uncontrolled (`defaultOpen`) usage via {@link useControllableState}.
  - exports: `UseDisclosureOptions`, `UseDisclosureReturn`, `useDisclosure`
  - styles: no
- **useDocumentTitle** — Sets `document.title` to `title`, keeping it in sync as `title` changes. SSR-safe: does nothing on the server (guards `document`). When `restoreOnUnmount` is set, the title present at mount time is restored when the component unmounts.
  - exports: `UseDocumentTitleOptions`, `useDocumentTitle`
  - styles: no
- **useElementSize** — Measures an element's size via `ResizeObserver`. Returns `[ref, { width, height }]`. Attach `ref` to the element you want to observe. SSR-safe (only observes inside the browser) and disconnects the observer on cleanup or when the ref detaches.
  - exports: `ElementSize`, `UseElementSizeReturn`, `useElementSize`
  - styles: no
- **useEventCallback** — Returns a callback with a stable identity that always invokes the latest `fn`. Combines `useCallback` with a ref so the returned function never changes between renders (safe to pass to memoized children or effect deps) while still calling the most up-to-date closure. The ref is updated in a layout effect so the latest `fn` is in place before paint.
  - exports: `useEventCallback`
  - styles: no
- **useEventListener** — Targets the listener can be attached to. Pass a ref to a DOM element, the element itself, `window`, `document`, or `null` (to skip).
  - exports: `EventTarget_`, `useEventListener`
  - styles: no
- **useFavicon** — Sets the page favicon to `href`. Reuses an existing `<link rel="icon">` if present, otherwise creates one and appends it to `<head>`. SSR-safe: does nothing on the server (guards `document`).
  - exports: `useFavicon`
  - styles: no
- **useFirstMountState** — Returns `true` only on the component's first render, `false` thereafter. Unlike an effect-based flag, this is correct during the render itself (the first render sees `true`), making it useful for skipping work that should only run on updates.
  - exports: `useFirstMountState`
  - styles: no
- **useFocusWithin** — Tracks whether focus is anywhere inside an element. Returns `[ref, isFocusWithin]`. Attach `ref` to the container; `isFocusWithin` is `true` while the element or any descendant holds focus. Subscribes to `focusin`/`focusout` and cleans up when the ref detaches.
  - exports: `UseFocusWithinReturn`, `useFocusWithin`
  - styles: no
- **useForceUpdate** — Returns a stable function that forces the component to re-render. Useful for bridging mutable/external state into React. Prefer real state where possible; reach for this only when a re-render must be triggered imperatively.
  - exports: `useForceUpdate`
  - styles: no
- **useFullscreen** — Fullscreen API binding for a single element. Returns `[ref, { isFullscreen, enter, exit, toggle }]`. Attach `ref` to the element to display fullscreen. SSR-safe and guarded against unsupported browsers; stays in sync with external changes via `fullscreenchange` and cleans the listener up.
  - exports: `FullscreenControls`, `UseFullscreenReturn`, `useFullscreen`
  - styles: no
- **useGeolocation** — Reads the user's geolocation via `navigator.geolocation`. SSR-safe: starts in a loading state and bails (with an error) when the API is unavailable. When `watch` is set, subscribes to position updates and clears the watch on unmount; otherwise performs a single read. Returns `{ loading, coords, error, timestamp }`.
  - exports: `GeolocationCoordinates`, `UseGeolocationState`, `UseGeolocationOptions`, `useGeolocation`
  - styles: no
- **useHotkeys** — Bind one or more keyboard combos to a handler. Combos use `+`-separated tokens, e.g. `"mod+k"`, `"shift+?"`, `"esc"`. `mod` matches Ctrl on Windows/Linux and Cmd on macOS. Pass a single string or an array of strings. SSR-safe: the listener is attached inside an effect and skipped where there is no DOM. The latest `handler` is always used, and the listener is removed on cleanup.
  - exports: `UseHotkeysOptions`, `useHotkeys`, `useKeyPress`
  - styles: no
- **useHover** — Tracks whether the pointer is over the referenced element. Attach the returned ref to a DOM node; `isHovered` flips on `mouseenter` / `mouseleave`. Listeners are bound in an effect and cleaned up on unmount or when the underlying node changes.
  - exports: `UseHoverReturn`, `useHover`
  - styles: no
- **useId** — SSR-safe unique id with an optional prefix. Thin wrapper over `React.useId` (React 18+), which produces matching ids on server and client to avoid hydration mismatches. Falls back to an incrementing counter on older React. Pass `prefix` for readable, scoped ids (e.g. `"nova-field"` → `"nova-field-:r3:"`).
  - exports: `useId`
  - styles: no
- **useIdle** — Returns `true` once the user has been idle for `timeout` ms. Any of the configured activity events (mouse move/down, key, touch, scroll, wheel) resets the timer back to "active". SSR-safe: subscribes inside an effect and guards `window`. Listeners and the pending timer are cleaned up on unmount.
  - exports: `UseIdleOptions`, `useIdle`
  - styles: no
- **useIntersectionObserver** — Observe an element's intersection with the viewport (or `root`). Returns `[ref, entry, isIntersecting]`. SSR-safe: the observer is only created in an effect and skipped where `IntersectionObserver` is unavailable. `root`, `rootMargin` and `threshold` pass straight through. The observer is disconnected on unmount or when options/node change.
  - exports: `UseIntersectionObserverOptions`, `UseIntersectionObserverReturn`, `useIntersectionObserver`
  - styles: no
- **useInterval** — Declarative `setInterval` (the Dan Abramov pattern). The `callback` is kept in a ref so it can change between renders without resetting the timer. Pass `delay = null` to pause. The interval is cleared on unmount or whenever `delay` changes.
  - exports: `useInterval`
  - styles: no
- **useIsomorphicLayoutEffect** — useLayoutEffect on the client, useEffect on the server. React warns when `useLayoutEffect` runs during SSR (it can't fire layout effects on the server). This hook swaps in `useEffect` when there is no DOM, silencing the warning while keeping synchronous layout behaviour in the browser.
  - exports: `useIsomorphicLayoutEffect`
  - styles: no
- **useLatest** — Returns a ref whose `.current` always holds the latest `value`. Useful inside effects, event handlers, or timers that close over a value but should read the most recent one without re-subscribing.
  - exports: `useLatest`
  - styles: no
- **useList** — Array state with common mutation helpers. Returns `{ list, set, push, removeAt, insertAt, updateAt, clear, move }`. All helpers are immutable (produce a new array) and stable across renders.
  - exports: `UseListReturn`, `useList`
  - styles: no
- **useLocalStorage** — Reactive state synced to `localStorage` under `key`. SSR-safe: renders `initialValue` on the server and during the first client render, then hydrates from storage in an effect (avoiding hydration mismatches). Stays in sync across tabs via the `storage` event. Values are JSON-serialized by default. Returns `[value, setValue, remove]`. `remove` deletes the key and resets the state to `initialValue`.
  - exports: `UseLocalStorageOptions`, `UseLocalStorageReturn`, `useLocalStorage`
  - styles: no
- **useLongPress** — Long-press gesture binding. Returns handler props to spread onto an element. `callback` fires once the pointer is held for `threshold` ms; the press is cancelled (firing `onCancel`) on pointer up/leave before the threshold, or when the pointer moves beyond `moveTolerance`. SSR-safe (timers only run client-side, inside event handlers).
  - exports: `UseLongPressOptions`, `LongPressHandlers`, `useLongPress`
  - styles: no
- **useMap** — `Map` state with helpers. Returns `{ map, set, get, has, remove, clear, reset }`. Mutations produce a fresh `Map` so React re-renders. Helper callbacks are stable across renders.
  - exports: `UseMapReturn`, `useMap`
  - styles: no
- **useMediaQuery** — Subscribe to a CSS media query and return whether it currently matches. SSR-safe: returns `defaultValue` (default `false`) when there is no `window.matchMedia`, then syncs to the real value after mount. Cleans up its listener on unmount or when the query string changes.
  - exports: `UseMediaQueryOptions`, `useMediaQuery`
  - styles: no
- **useMergedRefs** — Merges multiple refs (callback refs and ref objects) into a single ref callback. Useful when a component needs both its own internal ref and a forwarded ref on the same node. Returns a stable callback (memoised on the provided refs) that assigns the node to every supplied ref. `null`/`undefined` refs are ignored.
  - exports: `useMergedRefs`
  - styles: no
- **useMount** — Runs `callback` once, after the component mounts. The callback identity is ignored after the first run (it never re-fires).
  - exports: `useMount`
  - styles: no
- **useNetworkState** — Tracks online/offline status from `navigator.onLine`. SSR-safe: defaults to `{ online: true }` on the server and during the first client render, then syncs after mount. Subscribes to `online`/`offline` events with cleanup.
  - exports: `NetworkState`, `useNetworkState`
  - styles: no
- **usePrevious** — Returns the value of `value` from the previous render. On the first render this returns `undefined`. The stored value updates in an effect after each commit, so during render you always see the prior value.
  - exports: `usePrevious`
  - styles: no
- **useQueue** — A FIFO queue backed by React state. `add` enqueues at the back; `remove` dequeues from the front and returns the removed item. `clear` empties the queue. `first`, `last`, and `size` are derived from the current contents.
  - exports: `UseQueueReturn`, `useQueue`
  - styles: no
- **useReducedMotion** — Returns `true` when the user has requested reduced motion via `prefers-reduced-motion: reduce`. SSR-safe: returns `false` on the server / before mount, then syncs to the real value and subscribes to changes (cleaning up on unmount). Built directly on `matchMedia` so it has no dependency on other hooks.
  - exports: `useReducedMotion`
  - styles: no
- **useRenderCount** — Returns the number of times the component has rendered, starting at `1` on the first render and incrementing on every subsequent render. Primarily a debugging aid for spotting unnecessary re-renders.
  - exports: `useRenderCount`
  - styles: no
- **useScrollDirection** — Reports the vertical scroll direction (`"up"` | `"down"`) of the window, or of a scrollable element when a `ref` is given. Movements smaller than `threshold` are ignored. Reads are throttled via `requestAnimationFrame`. SSR-safe: starts as `null` and resolves after the first scroll past the threshold. The listener and any pending frame are cleaned up on unmount.
  - exports: `ScrollDirection`, `UseScrollDirectionOptions`, `useScrollDirection`
  - styles: no
- **useScrollLock** — Lock and unlock body scroll (e.g. while a modal or drawer is open). SSR-safe: all `document` access is guarded. Uses a shared reference count so nested locks compose correctly — the body only restores once every consumer has unlocked. Any active lock owned by this hook is released on unmount.
  - exports: `UseScrollLockOptions`, `UseScrollLockControls`, `useScrollLock`
  - styles: no
- **useScrollPosition** — Tracks the scroll position of the window, or of a scrollable element when a `ref` is provided. Reads are throttled via `requestAnimationFrame` so updates stay in sync with paint and never fire more than once per frame. SSR-safe: returns `{ x: 0, y: 0 }` on the server and reads the real position after mount. The scroll listener and any pending frame are cleaned up on unmount.
  - exports: `ScrollPosition`, `useScrollPosition`
  - styles: no
- **useSessionStorage** — Reactive state synced to `sessionStorage` under `key`. SSR-safe: renders `initialValue` on the server and during the first client render, then hydrates from storage in an effect (avoiding hydration mismatches). Values are JSON-serialized by default. Unlike `useLocalStorage`, `sessionStorage` is per-tab and not shared across tabs, so no `storage` listener is registered. Returns `[value, setValue, remove]`. `remove` deletes the key and resets the state to `initialValue`.
  - exports: `UseSessionStorageOptions`, `UseSessionStorageReturn`, `useSessionStorage`
  - styles: no
- **useSet** — `Set` state with helpers. Returns `{ set, add, remove, toggle, has, clear, reset }`. Mutations produce a fresh `Set` so React re-renders. Helper callbacks are stable across renders.
  - exports: `UseSetReturn`, `useSet`
  - styles: no
- **useStateHistory** — Stateful value with undo/redo history. Returns `{ state, set, undo, redo, canUndo, canRedo, history }`. Each `set` records a new entry and clears the redo stack; `undo`/`redo` walk the stack. Action callbacks are stable across renders.
  - exports: `UseStateHistoryReturn`, `useStateHistory`
  - styles: no
- **useStep** — Wizard / stepper state for a fixed number of steps. Returns `{ step, next, prev, goTo, reset, canNext, canPrev, isFirst, isLast }`. `count` is the total number of steps. Action callbacks are stable across renders.
  - exports: `UseStepReturn`, `useStep`
  - styles: no
- **useThrottledCallback** — Returns a throttled version of `callback` that runs at most once every `delay` ms (leading + trailing edge). The first call fires immediately; further calls within the window are coalesced and the most recent arguments fire on the trailing edge. The latest `callback` is always used (kept in a ref) so the returned identity is stable. Any pending trailing call is cancelled on unmount.
  - exports: `ThrottledFunction`, `useThrottledCallback`
  - styles: no
- **useThrottledValue** — Returns a throttled copy of `value` that updates at most once every `delay` ms (leading + trailing edge). The leading edge fires immediately on the first change; subsequent rapid changes are coalesced and the latest value is emitted on the trailing edge once the cooldown elapses. The pending trailing timer is cleared on unmount.
  - exports: `useThrottledValue`
  - styles: no
- **useTimeout** — Declarative `setTimeout`. The `callback` is kept in a ref so it can change without restarting the timer. Pass `delay = null` to disable. Returns `{ reset, clear }` for imperative control. The timeout is cleared on unmount or when `delay` changes.
  - exports: `UseTimeoutControls`, `useTimeout`
  - styles: no
- **useToggle** — Boolean state helper. Returns `[on, { toggle, on, off, set }]`. The actions object is stable across renders (the setters never change identity).
  - exports: `UseToggleActions`, `UseToggleReturn`, `useToggle`
  - styles: no
- **useUnmount** — Runs `callback` once, when the component unmounts. Always invokes the latest callback without re-subscribing.
  - exports: `useUnmount`
  - styles: no
- **useUnmountEffect** — Runs `callback` exactly once, when the component unmounts. The latest `callback` is kept in a ref so it can change between renders without re-subscribing, and only the final version runs on teardown.
  - exports: `useUnmountEffect`
  - styles: no
- **useUpdateEffect** — Like `useEffect`, but skips the first (mount) invocation. The effect runs only on updates when `deps` change. Cleanup behaves the same as `useEffect`.
  - exports: `useUpdateEffect`
  - styles: no
- **useWindowScroll** — Tracks the window scroll position and exposes a `scrollTo` helper. Reads are throttled via `requestAnimationFrame`. SSR-safe: returns `{ x: 0, y: 0 }` on the server and syncs the real position after mount. The scroll listener and any pending frame are cleaned up on unmount. Returns `{ x, y, scrollTo }`.
  - exports: `WindowScrollPosition`, `UseWindowScrollReturn`, `useWindowScroll`
  - styles: no
- **useWindowSize** — Tracks the viewport dimensions. SSR-safe: returns `{ width: undefined, height: undefined }` on the server and during the first client render, then measures after mount. Subscribes to `resize` with cleanup; pass `debounce` to throttle rapid resize events.
  - exports: `WindowSize`, `UseWindowSizeOptions`, `useWindowSize`
  - styles: no
- **VisuallyHidden** — Hides content visually while keeping it available to assistive technology. Wraps the shared `.nova-visually-hidden` utility (defined in base.css) as a polymorphic component. Use for screen-reader-only labels, skip links, live region text, etc.
  - exports: `VisuallyHiddenElement`, `VisuallyHiddenProps`, `VisuallyHidden`
  - styles: no
